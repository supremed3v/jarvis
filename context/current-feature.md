# Current Feature

## Working In

Not Started

## Status

Not Started

## Goals

-

## Dependencies

-

## Notes

-

## History

- 2026-08-01 SPEC-0010 Internal Message Protocol — Completed. Implemented packages/shared-types/message.go: Message, the internal communication envelope (ID, Timestamp, Source, Destination, Type, Payload) shared across Core Runtime components — the same "framework-independent, serializable data contract" pattern SPEC-0004 established for Event/Task/Agent/Tool, extended here in the Runtime phase since SPEC-0010 itself introduces no new type until now. MessageType is a closed 3-value enum (agent_communication, tool_request, event_notification) covering exactly the three categories the spec requires; Destination is `omitempty` so broadcast-style traffic (e.g. an event notification with no single addressed recipient) doesn't need to fake one. Deliberately scoped to the envelope shape only — no dispatcher, router, or Container wiring was added, since routing/delegation semantics belong to SPEC-0025 Agent Communication Protocol and permission-checked execution belongs to SPEC-0046 Tool Execution Engine, neither of which is implemented yet. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules; `gofmt -l` clean on the new file (message.go). types_test.go gained TestMessage_JSONRoundTrip, TestMessage_TypeValues, TestMessage_EmptyDestinationAndPayloadOmitted, and Message coverage in TestUnmarshal_MalformedJSONFailsWithoutPanic, matching the existing Event/Task/Agent/Tool test style. Built on feature/internal-message-protocol, merged into master. Full rationale: see docs/agents/JARVIS_BUILD_TRACKER.md (SPEC-0010 row).
- 2026-08-01 SPEC-0009 Event Bus — Completed. Implemented services/core/eventbus.go: Bus, the concrete implementation filling the Container.EventBus slot SPEC-0008 reserved as a placeholder `interface{}`. EventBus interface exposes Publish(event) and Subscribe(eventType, handler) (unsubscribe func()); Bus delivers to each subscriber on that subscriber's own dedicated goroutine reading off a bounded buffered channel (default 64, overridable via WithSubscriberBufferSize), so Publish itself never blocks — a full subscriber buffer drops that subscriber's copy of the event (and logs a warning if WithBusLogger is set) rather than stalling the publisher or other subscribers. Handler invocation is wrapped in a recover() (deliver()) so one panicking subscriber can't take down its goroutine or the process. Close(ctx) stops every subscription's delivery goroutine and waits for them to drain (or ctx to be done, whichever first), returning a packages/errors-wrapped EVENTBUS_CLOSE_INCOMPLETE error (Type TIMEOUT or CANCELED depending on ctx.Err()) on the latter, consistent with runtime.go's error-wrapping convention; Close and unsubscribe are both idempotent (sync.Once-guarded) and safe for concurrent use. Bus also implements Name()/Init() alongside Close() so it optionally satisfies runtime.Dependency for ordered startup/shutdown via WithDependencies, without EventBus itself requiring that — additive, runtime.go was not modified. Defines the three example EventTypes named in the spec (EventUserMessageReceived, EventAgentStarted, EventTaskCompleted), fulfilling the SPEC-0004 note that concrete event names belong to "future services (SPEC-0009 Event Bus onward)". container.go's placeholder `type EventBus interface{}` was removed in favor of this real interface; container_test.go's fake EventBus value (an anonymous struct that no longer satisfies the real interface) was replaced with a real NewBus(). A review pass caught and fixed two correctness issues before completion: (1) wg.Add(1) was originally called after Subscribe released its lock, racing a concurrent Close's wg.Wait() — a sync.WaitGroup misuse that can panic; fixed by moving Add inside the same locked section as the closed check, with a 200-iteration regression test; (2) handler panics were originally unrecovered; fixed via deliver()'s recover(), with a test proving a panicking subscriber doesn't block delivery to a healthy one and that its own goroutine survives to receive the next event. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (98.9% statement coverage on services/core, 21 new tests); `gofmt -l` clean on both new files. `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted under SPEC-0005/0007. Built on feature/event-bus, merged into master. Full rationale: see docs/agents/JARVIS_BUILD_TRACKER.md (SPEC-0009 row).
- 2026-08-01 SPEC-0008 Dependency Container — Completed. Implemented services/core/container.go: a Container struct managing Config, Logger, EventBus, TaskManager, ToolRegistry, and AgentRegistry as instance-scoped fields — no package-level state anywhere, satisfying the spec's "avoid global state" requirement directly. Config and Logger are wired to their real SPEC-0003/SPEC-0005 types via NewContainer(cfg, log, opts...); EventBus, TaskManager, ToolRegistry, and AgentRegistry are empty placeholder interfaces (`interface{}`) set only through ContainerOption functional options (WithEventBus/WithTaskManager/WithToolRegistry/WithAgentRegistry), mirroring the Option pattern already used by services/core/runtime.go and packages/logger. Scoped this way deliberately: SPEC-0009 Event Bus, the Task Execution specs (SPEC-0011..0017), SPEC-0045 Tool Registry, and SPEC-0020 Agent Registry are all still Planned, and a Phase 2 dependency container is expected to define typed slots for later-phase components rather than wire in implementations that don't exist yet — confirmed with the user before implementation rather than assumed. services/core/runtime.go was left untouched; Container is additive and not wired into Runtime's own lifecycle, since nothing yet consumes it that way. container_test.go (100.0% statement coverage) covers Config/Logger wiring, unwired slots defaulting to nil, options correctly wiring stub slots, and two Container instances holding independent state (proving no global state). `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules; `gofmt -l` clean on both new files. `gofmt -l .` across the wider workspace flags pre-existing files (e.g. runtime.go, config.go) as needing reformatting, but `gofmt -d` shows this is purely a CRLF/LF line-ending artifact of this Windows checkout's autocrlf setting, not a real formatting regression — no pre-existing file content was touched by this change. Built on feature/dependency-container, merged into master. Full rationale: see docs/agents/JARVIS_BUILD_TRACKER.md (SPEC-0008 row).
- 2026-08-01 SPEC-0007 Go Runtime Bootstrap — Completed. Implemented services/core (new Go module, module jarvis-pa/services/core, go 1.23) as the Runtime lifecycle owner for the Core Runtime component. Added root go.work (joining packages/config, packages/errors, packages/logger, packages/shared-types, services/core) — the root-level workspace anticipated since SPEC-0001's note that "no root-level go.mod/go.work exists until SPEC-0007 Go Runtime Bootstrap ties the modules together" — so Runtime can import the Foundation packages directly without publishing them or hand-written replace directives. Runtime (runtime.go) exposes New(opts...)/State()/Start(ctx)/Shutdown(ctx)/Run(ctx): Start loads config via packages/config.Load (file + env layering, unchanged), parses the resulting log level via packages/logger.ParseLevel and constructs a component-scoped Logger, then initializes each registered Dependency (a minimal Name/Init/Close interface satisfying the "Dependency initialization" requirement) in order — on any failure it rolls back the already-initialized subset in reverse order, transitions to StateFailed, and returns a packages/errors-wrapped error carrying the failing dependency's name as Context. Shutdown tears down the running dependency set in reverse order, collecting (rather than stopping at) every Close error so one broken dependency can't block the others from releasing resources, and only succeeds from StateRunning. Run composes Start with a caller-supplied context so OS signal handling (signal.NotifyContext) stays out of the library and in a future cmd/ entrypoint. State machine: NOT_STARTED -> STARTING -> RUNNING -> STOPPING -> STOPPED, with FAILED on any Start error; STOPPED may Start again (restart supported). `go build ./...`, `go vet ./...`, `gofmt -l .` clean; `go test ./... -v` 13/13 tests pass at 100.0% statement coverage, covering clean startup/shutdown, dependency init ordering, rollback-on-failure, invalid config file, invalid log level, double-start rejection, restart-after-shutdown, best-effort multi-error shutdown, context-driven Run (including Run's own Start-failure path), and the packages/config env-override integration boundary (LOG_LEVEL). Re-verified all 5 workspace modules build/test clean under the new go.work — no regressions in the standalone Foundation packages. `go test -race` unavailable in this environment (CGO_ENABLED requires cgo, no C toolchain present), same constraint noted under SPEC-0005. Built on feature/go-runtime-bootstrap, merged into master. Full rationale: see docs/agents/JARVIS_BUILD_TRACKER.md (SPEC-0007 row).
- 2026-08-01 SPEC-0006 Error Handling System — Completed. Implemented packages/errors (standalone Go module, module jarvis-pa/packages/errors, go 1.23, stdlib-only, no go.sum), following the packages/config (SPEC-0003) / packages/shared-types (SPEC-0004) / packages/logger (SPEC-0005) precedent for Foundation-layer packages. Implements Type (type.go) — a closed 10-value taxonomy (UNKNOWN, INVALID_INPUT, NOT_FOUND, ALREADY_EXISTS, PERMISSION_DENIED, UNAUTHENTICATED, UNAVAILABLE, TIMEOUT, CANCELED, INTERNAL) for programmatic classification — and Error (error.go): a structured error carrying Type, a stable free-form Code identifying the exact failure site, Component, Message, arbitrary Context, and an optional wrapped Cause. New/Wrap/Wrapf construct it; Error()/Unwrap() implement the standard error contract so errors.Is/errors.As traverse the full chain, including chains passing through plain stdlib fmt.Errorf(%w) wrapping; With(key, value) returns a context-augmented copy without mutating the original; Is/HasCode are package-level chain-walking helpers. Report (error.go) is a JSON-serializable reporting form (Timestamp, Type, Code, Component, Message, Context, Causes) satisfying the spec's "reporting format" requirement, with Causes flattening the full wrap chain into readable strings — no cross-module dependency on packages/logger was introduced (no go.work exists yet, each Foundation package remains standalone, matching packages/logger's own precedent of not depending on packages/shared-types). 18 tests cover construction, message formatting, cause preservation, Is/HasCode chain traversal (including through stdlib %w wrapping and against nil/plain errors), With immutability and chaining, Report field population and cause-chain flattening, and JSON marshal/unmarshal round-tripping. `go build ./...`, `go vet ./...`, `go test ./... -v`, `gofmt -l .` all clean (18/18 tests pass); `go test -cover` reports 100.0% statement coverage. Built on feature/error-handling-system, merged into master via commit 9fefd57 (merge commit follows feat commit aae0542). Full rationale: see docs/agents/JARVIS_BUILD_TRACKER.md (SPEC-0006 row).
- 2026-08-01 SPEC-0005 Logging System — Completed. Implemented packages/logger (standalone Go module, module jarvis-pa/packages/logger, go 1.23, stdlib-only, no go.sum), following the packages/config (SPEC-0003) / packages/shared-types (SPEC-0004) precedent for Foundation-layer packages. Implements Level (DEBUG/INFO/WARN/ERROR) with ParseLevel for config-driven parsing, Entry{Timestamp, Level, Component, Message, Metadata} as the structured record shape (Metadata is map[string]any with omitempty, matching the shared-types Event.Payload convention), and Logger — a component-scoped, mutex-protected writer (New(component, opts...) with WithOutput/WithMinLevel options, defaulting to os.Stdout and LevelDebug) emitting one JSON line per call via Debug/Info/Warn/Error(message, metadata). No dependency on packages/shared-types was needed; logging is a leaf concern with its own minimal shape. 8 tests cover required-fields presence, all four levels tagging entries correctly, metadata omission when nil, min-level filtering, ParseLevel success/failure, and 50 concurrent goroutines producing well-formed non-interleaved JSON lines. `go build ./...`, `go vet ./...`, `go test ./... -v`, `gofmt -l .` all clean (8/8 tests pass); `go test -race` could not run in this environment (CGO_ENABLED=0, no C toolchain). Full rationale: see docs/agents/JARVIS_BUILD_TRACKER.md (SPEC-0005 row).
- 2026-08-01 SPEC-0004 Shared Types Package — Completed. Implemented packages/shared-types (standalone Go module, module jarvis-pa/packages/shared-types, go 1.23, stdlib-only, no go.sum), following the packages/config (SPEC-0003) precedent for Foundation-layer packages. Defines four framework-independent, serializable data contracts with no business logic: Event (id/type/source/timestamp/payload), Task (id/type/status via TaskStatus enum/input/result/error/timestamps), Agent (id/name/type/status via AgentStatus enum/capabilities), Tool (name/description/parameters/permissions). EventType intentionally has no hardcoded constants, since concrete event names are producer-specific and belong to future services (SPEC-0009 Event Bus onward), not to this shared contract. 12 tests cover JSON round-trips for all four types, omitempty/zero-value edge cases, malformed-JSON error handling (fails safely, never panics), and cross-service wire-compatibility (unknown fields ignored for forward-compat, enums encode as plain JSON strings for non-Go consumers). `go build ./...`, `go vet ./...`, `go test ./... -v`, `gofmt -l .` all clean (12/12 tests pass); coverage reports "no statements" as expected for a types-only package. Built on feature/shared-types-package, merged into master via commit 3c99277 (merge commit follows). Full rationale: see docs/agents/JARVIS_BUILD_TRACKER.md (SPEC-0004 row).
