# JARVIS Build Tracker

## Purpose

Track implementation progress across all specifications.

## Status Values

-   Planned
-   In Progress
-   Blocked
-   Completed
-   Verified

## Tracking Structure

  Spec        Status      Owner   Notes
  ----------- ----------- ------- -------
  SPEC-0001   Completed           Structure created and validated (scripts/validate_structure.ps1: 14/14). Existing scaffold (packages/config, packages/logger, packages/shared-types, services/memory, services/tools, services/voice) kept authoritative over SPEC-0001's literal tree; configs/ and specs/ added to close the remaining gap — divergence approved by user, recorded in context/current-feature.md history. Build/test steps (go build/go test) not applicable at the repository-root level yet — no root-level go.mod/go.work exists until SPEC-0007 Go Runtime Bootstrap ties the modules together. This does not block individual Foundation-layer packages from having their own standalone go.mod: see SPEC-0003 below, which added one for packages/config since SPEC-0007 itself depends on that package already existing and being buildable/testable in isolation.
  SPEC-0002   Completed         Toolchain setup only (.go-version=1.23, .nvmrc=20, .env.example, scripts/verify_dev_environment.ps1, docs/DEVELOPMENT.md) — no go.mod/package.json/app code, per JARVIS_IMPLEMENTATION_ORDER.md (those arrive in SPEC-0007/SPEC-0063). Testing criteria 2 ("desktop launches") and 3 ("core runtime starts") rescoped to toolchain smoke-checks (node/npm present; go version present) since no Electron app or Go runtime exists yet — full criteria deferred to SPEC-0007/SPEC-0063. verify_dev_environment.ps1 run locally: Go 1.26.2, Node v24.15.0, npm 11.12.1 detected (all newer than pins — warning only, not failure); Ollama not installed locally (warning only). .env.example includes an optional NVIDIA_API_KEY/NVIDIA_API_BASE_URL pair reserved for a future hybrid local+cloud LLM path (not wired to code; ADR-0004 unchanged, Ollama remains the only active runtime).
  SPEC-0003   Completed         packages/config: standalone Go module (module jarvis-pa/packages/config, go 1.23, stdlib-only, no go.sum) — independent of the SPEC-0007 Go Runtime Bootstrap module, since SPEC-0007 lists "Configuration loading" as something it wires up (implying this package must exist and be buildable/testable on its own first). Implements Config{App, Model, Tools, Features} and Load(path) layering defaults -> optional JSON file -> env var overrides (JARVIS_ENV, LOG_LEVEL, OLLAMA_HOST, OLLAMA_PORT, NVIDIA_API_KEY, NVIDIA_API_BASE_URL) -> validation; fails safely (returns error, never a partial Config, never panics) on malformed file, bad env value, or out-of-range value. JSON chosen over YAML to avoid a third-party dependency for the repo's first Go module. `go build ./...`, `go vet ./...`, `go test ./... -v` all pass (7/7 tests). Also fixed scripts/generate_feature_index.ps1, which had hardcoded `Status: Planned` for every spec unconditionally — it now reads real status from this tracker (regex on the Status Values enum below), so FEATURE_INDEX.md no longer falsely shows completed specs as Planned.
  SPEC-0004   Completed         packages/shared-types: standalone Go module (module jarvis-pa/packages/shared-types, go 1.23, stdlib-only, no go.sum), following the packages/config precedent for Foundation-layer packages. Defines four framework-independent, serializable data contracts with no business logic: Event (id/type/source/timestamp/payload), Task (id/type/status via TaskStatus enum/input/result/error/timestamps), Agent (id/name/type/status via AgentStatus enum/capabilities), Tool (name/description/parameters/permissions). EventType intentionally has no hardcoded constants — concrete event names are producer-specific and belong to future services (SPEC-0009 Event Bus onward), not to this shared contract. `go build ./...`, `go vet ./...`, `go test ./... -v`, `gofmt -l .` all clean (12/12 tests pass); coverage reports "no statements" as expected for a types-only package. Tests cover JSON round-trips for all four types, omitempty/zero-value edge cases, malformed-JSON error handling (fails safely, never panics), and cross-service wire-compatibility (unknown fields ignored for forward-compat, enums encode as plain JSON strings).
  SPEC-0005   Completed         packages/logger: standalone Go module (module jarvis-pa/packages/logger, go 1.23, stdlib-only, no go.sum), following the packages/config and packages/shared-types precedent for Foundation-layer packages. Implements Level (DEBUG/INFO/WARN/ERROR) with ParseLevel for config-driven parsing, Entry{Timestamp, Level, Component, Message, Metadata} as the structured record shape, and Logger — a component-scoped, mutex-protected writer (New(component, opts...) with WithOutput/WithMinLevel options, defaulting to os.Stdout and LevelDebug) that emits one JSON line per call via Debug/Info/Warn/Error(message, metadata). Metadata is `map[string]any` with `omitempty`, matching the shared-types Event.Payload convention. No dependency on packages/shared-types was needed — logging is a leaf concern with its own minimal shape. `go build ./...`, `go vet ./...`, `go test ./... -v`, `gofmt -l .` all clean (8/8 tests pass); `go test -race` could not run in this environment (CGO_ENABLED=0, no C toolchain) but the write path is a single mutex-guarded `io.Writer.Write` call per entry, so races are not expected. Tests cover required-fields presence, all four levels tagging entries correctly, metadata omission when nil, min-level filtering, ParseLevel success/failure, and 50 concurrent goroutines producing 50 well-formed, non-interleaved JSON lines.
  SPEC-0006   Completed         packages/errors: standalone Go module (module jarvis-pa/packages/errors, go 1.23, stdlib-only, no go.sum), following the packages/config, packages/shared-types, and packages/logger precedent for Foundation-layer packages. Implements Type (type.go) — a closed 10-value taxonomy (UNKNOWN, INVALID_INPUT, NOT_FOUND, ALREADY_EXISTS, PERMISSION_DENIED, UNAUTHENTICATED, UNAVAILABLE, TIMEOUT, CANCELED, INTERNAL) for programmatic classification — and Error (error.go): a structured error carrying Type, a stable free-form Code identifying the exact failure site, Component, Message, arbitrary Context, and an optional wrapped Cause. New/Wrap/Wrapf construct it; Error()/Unwrap() implement the standard error contract so errors.Is/errors.As traverse the full chain, including through plain stdlib fmt.Errorf(%w) wrapping; With(key, value) returns a context-augmented copy without mutating the original; Is/HasCode are package-level chain-walking helpers. Report is a JSON-serializable reporting form (Timestamp, Type, Code, Component, Message, Context, Causes) satisfying the "reporting format" requirement, with Causes flattening the full wrap chain into readable strings — no cross-module dependency on packages/logger was introduced, since no go.work exists yet and each Foundation package remains standalone (matching packages/logger's own precedent of not depending on packages/shared-types). `go build ./...`, `go vet ./...`, `go test ./... -v`, `gofmt -l .` all clean (18/18 tests pass); `go test -cover` reports 100.0% statement coverage. Tests cover construction, message formatting, cause preservation, Is/HasCode chain traversal (including through stdlib %w wrapping and against nil/plain errors), With immutability and chaining, Report field population and cause-chain flattening, and JSON marshal/unmarshal round-tripping.
  SPEC-0007   Completed         services/core: new Go module (module jarvis-pa/services/core, go 1.23) implementing the Runtime lifecycle owner for the Core Runtime component. Added root go.work (joining packages/config, packages/errors, packages/logger, packages/shared-types, services/core) — the root-level workspace anticipated since SPEC-0001's note that "no root-level go.mod/go.work exists until SPEC-0007 Go Runtime Bootstrap ties the modules together" — so Runtime can import the Foundation packages directly without publishing them or hand-writing replace directives. Runtime (runtime.go) exposes New(opts...)/State()/Start(ctx)/Shutdown(ctx)/Run(ctx): Start loads config via packages/config.Load (file + env layering, unchanged), parses the resulting log level via packages/logger.ParseLevel and constructs a component-scoped Logger, then initializes each registered Dependency (a minimal Name/Init/Close interface satisfying the "Dependency initialization" requirement) in order — on any failure it rolls back the already-initialized subset in reverse order, transitions to StateFailed, and returns a packages/errors-wrapped error carrying the failing dependency's name as Context. Shutdown tears down the running dependency set in reverse order, collecting (rather than stopping at) every Close error so one broken dependency can't block the others from releasing resources, and only succeeds from StateRunning. Run composes Start with a caller-supplied context so OS signal handling (signal.NotifyContext) stays out of the library and in a future cmd/ entrypoint. State machine: NOT_STARTED -> STARTING -> RUNNING -> STOPPING -> STOPPED, with FAILED on any Start error; STOPPED may Start again (restart supported). `go build ./...`, `go vet ./...`, `gofmt -l .` clean; `go test ./... -v` 13/13 tests pass at 100.0% statement coverage, covering clean startup/shutdown, dependency init ordering, rollback-on-failure, invalid config file, invalid log level, double-start rejection, restart-after-shutdown, best-effort multi-error shutdown, context-driven Run (including Run's own Start-failure path), and the packages/config env-override integration boundary (LOG_LEVEL). Re-verified all 5 workspace modules build/test clean under the new go.work — no regressions in the standalone Foundation packages. `go test -race` unavailable in this environment (CGO_ENABLED requires cgo, no C toolchain present), same constraint noted under SPEC-0005.
  SPEC-0008   Completed         services/core/container.go: Container struct managing Config, Logger, EventBus, TaskManager, ToolRegistry, and AgentRegistry as instance-scoped fields (no package-level state). Config and Logger are wired to their real SPEC-0003/SPEC-0005 types via the NewContainer(cfg, log, opts...) constructor; EventBus, TaskManager, ToolRegistry, and AgentRegistry are empty placeholder interfaces (`interface{}`) set only through ContainerOption functional options (WithEventBus/WithTaskManager/WithToolRegistry/WithAgentRegistry), mirroring the Option pattern already used by services/core/runtime.go and packages/logger — scoped this way on purpose, since SPEC-0009 Event Bus, the Task Execution specs (SPEC-0011..0017), SPEC-0045 Tool Registry, and SPEC-0020 Agent Registry are all still Planned and a Phase 2 container is expected to define typed slots for later-phase components rather than wire in implementations that don't exist yet (confirmed with user before implementation). runtime.go was left untouched — Container is additive, not wired into Runtime's own lifecycle, since that wiring isn't needed until a later spec consumes it. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules; container_test.go (100.0% statement coverage) covers Config/Logger wiring, unwired slots defaulting to nil, options correctly wiring stub slots, and two Container instances holding independent state. `gofmt -l` on the two new files (container.go, container_test.go) is clean; `gofmt -l .` across the wider workspace flags pre-existing files (e.g. runtime.go, config.go) as needing reformatting, but `gofmt -d` shows this is purely a CRLF/LF line-ending difference from this Windows checkout's autocrlf setting, not a real formatting regression — no file content besides the two new ones was touched, so this is a pre-existing environment artifact, not introduced by SPEC-0008.
  SPEC-0009   Completed         services/core/eventbus.go: Bus, the concrete implementation filling the Container.EventBus slot SPEC-0008 reserved as a placeholder `interface{}`. EventBus interface exposes Publish(event) and Subscribe(eventType, handler) (unsubscribe func()); Bus delivers to each subscriber on that subscriber's own dedicated goroutine reading off a bounded buffered channel (default 64, overridable via WithSubscriberBufferSize), so Publish itself never blocks — a full subscriber buffer drops that subscriber's copy of the event (and logs a warning if WithBusLogger is set) rather than stalling the publisher or other subscribers. Handler invocation is wrapped in a recover() (deliver()) so one panicking subscriber can't take down its goroutine or the process. Close(ctx) stops every subscription's delivery goroutine and waits for them to drain (or ctx to be done, whichever first), returning a packages/errors-wrapped EVENTBUS_CLOSE_INCOMPLETE error (Type TIMEOUT or CANCELED depending on ctx.Err()) on the latter, consistent with runtime.go's error-wrapping convention; Close and unsubscribe are both idempotent (sync.Once-guarded) and safe for concurrent use. Bus also implements Name()/Init() alongside Close() so it optionally satisfies runtime.Dependency for ordered startup/shutdown via WithDependencies, without EventBus itself requiring that — this is additive, runtime.go was not modified. Defines the three example EventTypes named in the spec (EventUserMessageReceived, EventAgentStarted, EventTaskCompleted), fulfilling the SPEC-0004 note that concrete event names belong to "future services (SPEC-0009 Event Bus onward)". container.go's placeholder `type EventBus interface{}` was removed in favor of this real interface; container_test.go's fake EventBus value (an anonymous struct that no longer satisfies the real interface) was replaced with a real NewBus(). A review pass caught and fixed two correctness issues before completion: (1) `wg.Add(1)` was originally called after Subscribe released its lock, racing a concurrent Close's `wg.Wait()` — a sync.WaitGroup misuse that can panic; fixed by moving Add inside the same locked section as the closed check, with a 200-iteration regression test (TestBus_SubscribeConcurrentWithCloseDoesNotRace); (2) handler panics were originally unrecovered; fixed via deliver()'s recover(), with TestBus_HandlerPanicDoesNotStopOtherSubscribers proving a panicking subscriber doesn't block delivery to a healthy one and that its own goroutine survives to receive the next event. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (98.9% statement coverage on services/core, 21 new tests covering publish/subscribe, multiple independent listeners, async/non-blocking delivery, buffer-overflow dropping, idempotent unsubscribe/Close, per-event-type isolation, and the two regression cases above); `gofmt -l` clean on both new files (eventbus.go, eventbus_test.go). `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted under SPEC-0005/0007. Built on feature/event-bus.
  SPEC-0010   Completed         packages/shared-types/message.go: Message, the internal communication envelope (ID, Timestamp, Source, Destination, Type, Payload) shared across Core Runtime components — the same "framework-independent, serializable data contract" pattern SPEC-0004 established for Event/Task/Agent/Tool, extended here in the Runtime phase since SPEC-0010 itself introduces no new type until now. MessageType is a closed 3-value enum (agent_communication, tool_request, event_notification) covering exactly the three categories the spec requires; Destination is `omitempty` so broadcast-style traffic (e.g. an event notification with no single addressed recipient) doesn't need to fake one. Deliberately scoped to the envelope shape only — no dispatcher, router, or Container wiring was added, since routing/delegation semantics belong to SPEC-0025 Agent Communication Protocol and permission-checked execution belongs to SPEC-0046 Tool Execution Engine, neither of which is implemented yet. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules; `gofmt -l` clean on the new file (message.go). types_test.go gained TestMessage_JSONRoundTrip, TestMessage_TypeValues, TestMessage_EmptyDestinationAndPayloadOmitted, and Message coverage in TestUnmarshal_MalformedJSONFailsWithoutPanic, matching the existing Event/Task/Agent/Tool test style. Built on feature/internal-message-protocol, merged into master.
  SPEC-0011   Completed         packages/shared-types/task.go: extends the `Task` struct reserved for SPEC-0011 during SPEC-0004 with `Title, Description, Source, Priority, ParentID, Metadata`, keeping the existing `Type/Input/Result/Error` fields as-is (additive extension). Adds a closed `TaskSource` enum (voice/desktop/agent/scheduled) covering exactly the four sources the spec requires. `TaskPriority` is a bare string type with no hardcoded levels — concrete priority levels (LOW/NORMAL/HIGH/CRITICAL) belong to SPEC-0013 Task Queue's "Priority Levels" section, not this data-model-only spec, mirroring the SPEC-0004 precedent of `EventType` having no hardcoded constants. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules; types_test.go gained expanded `TestTask_JSONRoundTrip` coverage plus `TestTask_SourceValues` and `TestTask_EmptyOptionalFieldsOmitted`, matching the existing Event/Task/Agent/Tool/Message test style. Built on feature/task-model.
  SPEC-0012   Completed         packages/shared-types/task.go: replaces the provisional 5-value `TaskStatus` (`pending/running/completed/failed/cancelled`, added under SPEC-0011 with a comment deferring the real enum to this spec) with the 8 states SPEC-0012 requires — `created, planning, queued, executing, waiting, failed, completed, cancelled` (lowercase, matching the `TaskSource`/`MessageType`/`AgentStatus` convention; confirmed with user since nothing outside shared-types referenced the old names). Adds `TaskTransition` (TaskID/From/To/Timestamp) as a shared data shape for a single recorded state change, mirroring the `Event` precedent. Transition *validation* and *history tracking* — real behavior, which SPEC-0004 explicitly keeps out of packages/shared-types ("shapes only, no business logic") — live instead in new `services/core/task_state_machine.go`: a closed `validTransitions` adjacency table (`created->{planning,cancelled}`, `planning->{queued,failed,cancelled}`, `queued->{executing,cancelled}`, `executing->{completed,waiting,failed,cancelled}`, `waiting->{executing,failed,cancelled}`, `completed/failed/cancelled` terminal), `CanTransition(from,to)`, and `StateMachine` (`Transition(task,to)` validates+mutates+records via `packages/errors` `TypeInvalidInput`/`TASK_INVALID_TRANSITION` on rejection; `History(taskID)` returns a defensive copy of that task's recorded transitions). Placed in `services/core` per `JARVIS_MASTER_ARCHITECTURE.md` ("Core Runtime: task execution") and `container.go`'s existing `TaskManager` placeholder comment ("for the Task Execution layer, SPEC-0011..0017"); `container.go` itself was not touched, since that slot covers the full multi-spec Task Execution layer, not SPEC-0012 alone. Transition graph beyond the spec's own `CREATED->PLANNING->QUEUED->EXECUTING->COMPLETED` example is this implementation's design choice, recorded in context/current-feature.md history, not mandated by the spec text. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules; a review pass caught the `StateMachine` doc comment overclaiming full concurrent-use safety (the `history` map is mutex-protected, but `Transition` mutates the caller's `*Task` outside the lock) — fixed by narrowing the doc comment to state that concurrent `Transition` calls on the *same* `*Task` need the caller's own synchronization. `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted under SPEC-0005/0007/0009. Built on feature/task-state-machine (off feature/task-model).
  SPEC-0013   Completed         services/core/task_queue.go: new `Queue` type filling the "Priority ordering" / "Adding tasks" / "Removing tasks" / "Queue inspection" / "Worker consumption" requirements of SPEC-0013. Concrete `TaskPriority` values (`PriorityLow/Normal/High/Critical`, string-backed) live here rather than in `packages/shared-types/task.go`, mirroring `eventbus.go`'s precedent of owning `EventType` constants outside the shape-only SPEC-0004 package — `task.go`'s own SPEC-0011 comment already deferred priority levels to this spec. `Queue` buckets queued `*types.Task` pointers by priority tier in a `map[types.TaskPriority][]*types.Task` guarded by a `sync.Mutex`: `Add` (rejects duplicate task IDs and unrecognized priorities via `packages/errors` `TypeAlreadyExists`/`TASK_QUEUE_DUPLICATE_TASK` and `TypeInvalidInput`/`TASK_QUEUE_INVALID_PRIORITY`; an unset `Priority` defaults to `PriorityNormal` without mutating the caller's `Task`), `Next` (pops highest-tier, FIFO-within-tier, for worker consumption; `TypeNotFound`/`TASK_QUEUE_EMPTY` when empty), `Remove` (dequeue by ID; `TypeNotFound`/`TASK_QUEUE_TASK_NOT_FOUND` when absent), `List` (priority-ordered inspection without removing), `Len`. Deliberately does not enforce `TaskStatus` transitions or integrate with `StateMachine` (SPEC-0012) — SPEC-0013's own Requirements/Testing sections don't ask for that, and `container.go`'s `TaskManager` placeholder was left untouched for the same reason SPEC-0012 left it alone (covers the whole multi-spec Task Execution layer, not one spec). `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (12 new tests: enqueue, duplicate-ID rejection, unknown-priority rejection, default-priority-to-Normal, priority-tier ordering, FIFO within a tier, empty-queue error, remove/remove-unknown, list-without-removing, empty-list, and an 8-worker/200-task concurrent drain proving exactly-once delivery). `gofmt -l` clean on both new files. `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted under SPEC-0005/0007/0009/0012. Built on feature/task-queue (off master, post SPEC-0012 merge).
  SPEC-0014   Completed         services/core/task_worker.go: new `Worker` type filling the "Pull tasks from queue" / "Execute assigned work" / "Update task status" / "Report failures" / "Emit events" requirements of SPEC-0014, tying together `Queue` (SPEC-0013), `StateMachine` (SPEC-0012), and `EventBus` (SPEC-0009) for the first time. `Worker.Start`/`Stop` follow `Bus`'s own lifecycle idiom exactly (context-derived cancellation, a `done` channel closed by the loop goroutine, `Stop` racing that channel against the caller's `ctx` and returning a `packages/errors` `TypeTimeout`/`TypeCanceled` `WORKER_STOP_INCOMPLETE` on the latter — mirroring `EVENTBUS_CLOSE_INCOMPLETE`); `Start` rejects a second concurrent start via `TypeInternal`/`WORKER_ALREADY_STARTED`, `Stop` is idempotent. The loop polls `Queue.Next()` (`WithPollInterval`-configurable, default 20ms) and hands each task to `processTask`, which transitions Queued->Executing via `StateMachine`, runs a caller-supplied `Executor func(ctx, *types.Task) (map[string]any, error)`, then transitions to Completed (result stored on `task.Result`) or Failed (error stored on `task.Error`) — publishing `TASK_STARTED`/`TASK_COMPLETED`/`TASK_FAILED` events (`TASK_COMPLETED` reused from `eventbus.go`; `TASK_STARTED`/`TASK_FAILED` are new constants owned here, mirroring `Queue` owning `TaskPriority`). A task that cannot validly transition to Executing (e.g. re-queued after already reaching a terminal status) is reported failed without ever reaching the `Executor`. `container.go`'s `TaskManager` placeholder was left untouched, for the same reason SPEC-0012/0013 left it alone. `go build ./...`, `go vet ./...`, `go test ./... -v`, `gofmt -l` all clean across all 5 go.work modules (10 new tests: success path with result + transition history, executor failure, TASK_STARTED/TASK_COMPLETED/TASK_FAILED event emission, invalid-starting-status failure without invoking the executor, empty-queue polling, Stop halting further pickup, double-Start rejection, idempotent Stop, and Stop timing out against a blocked executor). `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted under SPEC-0005/0007/0009/0012/0013. A review pass noted two non-blocking, precedent-consistent edge cases for later specs to be aware of: (1) `Task.Status/Result/Error` are mutated by the worker goroutine with no synchronization for concurrent external readers, matching `StateMachine.Transition`'s already-documented single-owner contract rather than introducing a new gap; (2) a task that fails to transition to Failed (already terminal) still publishes `TASK_FAILED` even though its `Status` field doesn't change, a defensive-only path reachable only via queue/state misuse. Built on feature/task-worker-system (off master, post SPEC-0013 merge).
  SPEC-0015   Completed         services/core/task_retry.go: new `RetryPolicy`/`RetryManager` filling the "Retry count" / "Retry delay" / "Maximum attempts" / "Failure reasons" requirements of SPEC-0015, plus its "retries must avoid infinite loops" rule. Retry bookkeeping (per-task attempt count and ordered failure-reason history, both keyed by task ID under a `sync.Mutex`) lives entirely in `RetryManager`, not on `types.Task` — the same pattern `StateMachine.history` already established — so `packages/shared-types/task.go` (SPEC-0011) and the `StateMachine`/`Queue` transition and enqueue logic (SPEC-0012/0013) needed no changes. `RecordFailure(taskID, reason)` increments the attempt count, appends the reason, and reports whether another attempt should be made (`attempt < MaxAttempts`) so the caller has a single locked decision point. `task_worker.go`'s `Worker` gained an opt-in `WithRetryManager` option (a `Worker` with none configured keeps SPEC-0014's original fail-on-first-error behavior unchanged); on an `Executor` error, `handleFailure` now consults the `RetryManager` and, if a retry is due, transitions the task Executing->Waiting (already a valid SPEC-0012 transition) and publishes the new `TASK_RETRY_SCHEDULED` event instead of `TASK_FAILED`, then `scheduleRetry` re-adds the task to the `Queue` on its own goroutine after `RetryManager.Delay()` elapses — letting a Worker already picking up Waiting->Executing (also pre-existing) drive the next attempt without any new state-machine transitions. Once `MaxAttempts` is exhausted the task falls through to the original `fail()` path (`Failed` transition + `TASK_FAILED`), so the retry reasons remain on the `RetryManager` as the recorded final-failure history. `Worker.Stop` now also waits for any in-flight `scheduleRetry` goroutines (tracked via a new `retryWG`) before returning, so a stopped Worker can't leave a retry racing in the background; a retry whose delay hasn't elapsed when `Stop` is called is abandoned with the task left in `Waiting` rather than silently completing after shutdown. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (71/71 tests pass; 7 new: `RetryManager` attempt/reason tracking, max-attempts cutoff, and `Reset`, plus `Worker` retry-until-success, terminal-failure-after-max-attempts with `TASK_RETRY_SCHEDULED`/`TASK_FAILED` event-count assertions, and a no-`RetryManager`-configured regression guarding SPEC-0014's original single-failure behavior). `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted under SPEC-0005/0007/0009/0012/0013/0014. Built on feature/task-retry-system (off master, post SPEC-0014 merge).
  SPEC-0016   Completed         services/core/task_scheduler.go: new `Scheduler` type filling the "One-time tasks" / "Recurring tasks" / "Delayed execution" requirements of SPEC-0016, producing `Task`s on a schedule rather than executing them — it drives each fired `Task` through `StateMachine` (SPEC-0012) `Created->Planning->Queued` transitions and hands it to `Queue.Add` (SPEC-0013) for an existing `Worker` (SPEC-0014) to pick up, so `Worker`/`Queue`/`StateMachine`/`RetryManager` (SPEC-0011-0015) needed no changes. Three constructors build a `Schedule` around a caller-supplied `TaskFactory func() *types.Task`: `ScheduleOnce(id, at, factory)` (fires once at a fixed time), `ScheduleAfter(id, delay, factory)` (fires once after a delay), `ScheduleEvery(id, interval, factory)` (repeats until canceled) — the `scheduleKind` discriminant is unexported so callers can't construct a `Schedule` with a mismatched kind/duration pair directly. `Scheduler.Start`/`Stop` follow `Worker`'s own lifecycle idiom (context-derived cancellation, a `done` channel raced against the caller's `ctx` in `Stop`, returning `packages/errors` `TypeTimeout`/`TypeCanceled` `SCHEDULER_STOP_INCOMPLETE` on timeout, `TypeInternal`/`SCHEDULER_ALREADY_STARTED` on double-start); a background `time.Ticker` (`WithSchedulerTick`-configurable, default 10ms) drives `fireDue`, which finds every due `Schedule` and advances (`kindEvery`) or removes (`kindOnce`/`kindAfter`) it under the Scheduler's own mutex before firing any of them outside that lock, so a concurrent `Cancel` can never race a firing already in flight. `Schedule`/`Cancel` validate input (`TypeInvalidInput`/`SCHEDULE_INVALID` for a missing ID/Factory or non-positive delay/interval, `TypeAlreadyExists`/`SCHEDULE_DUPLICATE_ID` for a duplicate ID, `TypeNotFound`/`SCHEDULE_NOT_FOUND` for an unknown cancel target) and publish new `TASK_SCHEDULED`/`SCHEDULED_TASK_FIRED`/`SCHEDULED_TASK_CANCELED` events on the `EventBus` (SPEC-0009), mirroring `Worker`'s precedent of owning its own event-type constants. A firing that fails (bad transition, or `Queue.Add` rejecting a duplicate task ID from a buggy `TaskFactory`) is logged and dropped rather than retried inline — a `kindEvery` schedule still fires again at its next interval regardless. `container.go`'s `TaskManager` placeholder was left untouched, for the same reason SPEC-0012-0015 left it alone. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (16 new tests: one-time/delayed/recurring firing, cancellation stopping future firings, event emission, input validation, duplicate-ID/unknown-cancel rejection, double-start/idempotent-stop, dynamic scheduling against an already-running Scheduler, failed-firing resilience against a colliding-ID `TaskFactory`, and a full `Scheduler`->`Queue`->`Worker` integration test proving a fired `Task` actually completes). `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted under SPEC-0005/0007/0009/0012/0013/0014/0015. Built on feature/background-scheduler (off master, post SPEC-0015 merge).
  SPEC-0017   Completed         services/core/task_history.go: new `HistoryStore` type filling the "Task execution timeline" / "Status changes" / "Tool executions" / "Errors" / "Results" requirements of SPEC-0017, the final piece of the Task Execution layer `container.go`'s `TaskManager` placeholder comment names (SPEC-0011..0017). Rather than adding a new write path, `HistoryStore` subscribes to the lifecycle events the layer already publishes — `TASK_SCHEDULED`/`SCHEDULED_TASK_FIRED`/`SCHEDULED_TASK_CANCELED` (SPEC-0016), `TASK_STARTED`/`TASK_COMPLETED`/`TASK_FAILED` (SPEC-0014), `TASK_RETRY_SCHEDULED` (SPEC-0015) — via `EventBus` (SPEC-0009) and records each one (`HistoryRecord{TaskID, EventType, Timestamp, Payload}`) keyed by the event's `taskId` payload field, so `Queue`/`StateMachine`/`RetryManager`/`Scheduler` (SPEC-0011-0013/0015/0016) needed no changes; the event type set is overridable via `WithHistoryEventTypes` so a later Task-scoped producer (e.g. a future Tool Execution Engine) can be historized without changing `HistoryStore` itself. One existing file needed a small change: `task_worker.go`'s `EventTaskCompleted` publish previously omitted the task's result from its payload (nothing before SPEC-0017 needed it), so a `"result": task.Result` key was added to satisfy the "Results" requirement — `Event.Payload` is already a free-form `map[string]any`, so this is an additive payload key, not a schema change. `History(taskID)` returns that task's recorded timeline sorted by each event's own `Timestamp` rather than delivery order, since different event types are delivered on independent per-subscription goroutines and so are not guaranteed to arrive in publish order (verified empirically: two `Publish` calls microseconds apart can race to delivery). A review pass caught and fixed one real bug before completion: `record()` originally stored `event.Payload` by reference rather than copying it, so a "persisted" `HistoryRecord` was actually still aliased to the same mutable map the publisher (or another subscriber) held — fixed with a shallow copy at record time, plus `TestHistoryStore_RecordedPayloadIsNotAliasedToPublishedPayload` as a regression guard. `container.go`'s `TaskManager` placeholder was left untouched, for the same reason SPEC-0012-0016 left it alone; `HistoryStore` is in-memory (a `map[string][]HistoryRecord]` under a `sync.Mutex`), matching every other Task Execution component's existing precedent, since no relational storage dependency exists anywhere in the repo yet despite ADR-0007 nominally calling Task data "Relational" — that gap is pre-existing across the whole layer, not introduced here. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (12 new/updated tests: recording, retrieval, unknown-task/nil-bus/no-taskId edge cases, `Close` semantics, custom event-type override, payload-non-aliasing, and 4 integration tests through real `Worker`/`RetryManager`/`Scheduler` covering full completed-task timeline with result payload, failed-task error payload, retry-then-completion, and scheduler-fired lifecycle). `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted under SPEC-0005/0007/0009/0012/0013/0014/0015/0016. Built on feature/task-history-storage (off master, post SPEC-0016 merge).
  SPEC-0018   Completed         services/core/agent.go: new `AgentMetadata`/`Agent` types filling the "Agent contract" requirements of SPEC-0018 - the first spec of Phase 4 Intelligence, built directly on the completed Phase 2/3 Runtime and Task Execution layers. `AgentMetadata` (ID/Name/Description/Instructions/Tools/MemoryAccess/Permissions) is the static, declarative half of the contract the runtime can inspect without invoking the agent; `Tools`/`MemoryAccess`/`Permissions` are plain `[]string` identifiers, not resolved capabilities, mirroring `TaskSource`/`TaskPriority`'s precedent of being bare strings that other not-yet-built specs (Tool Registry SPEC-0043..0045, the memory system, a future permission system) own the meaning of - so this spec does not depend on those siblings existing first, resolving the ambiguity in `JARVIS_DEPENDENCY_GRAPH.md`'s "Critical Dependencies" section (which lists Agents as requiring LLM/Tools/Memory) in favor of the phase ordering in `JARVIS_IMPLEMENTATION_ORDER.md` (Agent Interface first within Phase 4). `AgentMetadata.Validate()` enforces the two required identity fields (ID, Name) via `packages/errors` `TypeInvalidInput`, satisfying the "Agent metadata validates correctly" testing criterion. `Agent` is a two-method interface - `Metadata() AgentMetadata` and `Execute(ctx, *types.Task) (map[string]any, error)` - deliberately not exposing separate Plan/tool-use/result-shaping methods beyond what SPEC-0018's Requirements list names, since "Agent Responsibilities" (understanding tasks, planning, tool use, structured results) is documented behavior an implementation's own `Execute` fulfills internally, not additional contract surface. `Execute`'s signature was chosen to exactly match the existing `Executor` type (`task_worker.go`, SPEC-0014), so a `Worker` can drive any conforming `Agent` via its `Execute` method value directly - no adapter needed - satisfying the "runtime can execute a sample agent" testing criterion by reusing the real `Queue`/`StateMachine`/`EventBus` wiring rather than a mock runtime. `packages/shared-types/agent.go`'s existing `Agent` struct (added under SPEC-0004, annotated for SPEC-0018/SPEC-0020) is the separate registry-record shape for a *registered* agent's live state (ID/Name/Type/Status/Capabilities) and was left untouched - it is SPEC-0020 Agent Registry's concern, not this behavioral contract's. `container.go`'s `AgentRegistry interface{}` placeholder (reserved for SPEC-0020) was also left untouched, for the same reason `TaskManager`/`ToolRegistry` were left alone by SPEC-0012-0017: this spec defines the contract those registries will hold instances of, not the registry itself. `agents/core-agent`/`agents/developer-agent`/`agents/research-agent` remain empty scaffolds (`.gitkeep` only) - no concrete agent implementation was added, since none is required or requested by SPEC-0018's own Requirements/Testing sections; `agent_test.go`'s `stubAgent` fills the "sample agent" testing role instead. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (3 new tests: interface implementability with Metadata/Execute round-tripping, a real `Queue`+`StateMachine`+`EventBus`+`Worker` integration proving a Worker can execute an `Agent` via its bound `Execute` method with no adapter, and table-driven `AgentMetadata.Validate` coverage for valid/missing-ID/missing-Name). `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted under SPEC-0005/0007/0009/0012/0013/0014/0015/0016/0017. Built on feature/agent-interface (off master, post SPEC-0017 merge).
  SPEC-0019   Completed         services/core/agent_manifest.go: new `Manifest` type filling the "configuration-based system for defining agents" requirements of SPEC-0019 - Identity (Name/Description), Capabilities, Tools, Permissions (`map[string]ManifestPermission`, e.g. `terminal: {require_confirmation: true}`), Model preferences (`ManifestModel`: Provider/Name/Temperature), and free-form Configuration (`map[string]any`), all YAML-tagged to match the spec's own `developer_agent` example exactly. `LoadManifest(path)` reads and parses the file then calls `Validate()` (requires a non-empty Name; rejects a `Permissions` entry for any tool not also listed under `Tools`, since a permission rule for a tool the agent never uses indicates a manifest error rather than something to silently ignore) - satisfying the "manifest loads correctly" / "invalid manifests fail validation" testing criteria. `Manifest.Metadata()` derives a SPEC-0018 `AgentMetadata` (ID/Name from the manifest Name, Tools passed through, Permissions flattened to the sorted set of gated tool names) without modifying `AgentMetadata` itself, since Capabilities/Model/Configuration have no equivalent slot in that already-`Completed` contract - they remain manifest-only fields for whatever later spec (LLM layer, a future permission system) needs them. `NewAgentFromManifest(m, execute Executor) (Agent, error)` builds a concrete `Agent` from a validated `Manifest` plus a caller-supplied `Executor` (same `func(ctx, *types.Task) (map[string]any, error)` type `task_worker.go`/SPEC-0014 already defines), satisfying "agents can be created from manifests" - a manifest is declarative only, so the actual Task-handling logic is still code the caller provides, mirroring SPEC-0018's own "planning and tool use are internal to Execute" precedent. Parses YAML via `gopkg.in/yaml.v3` - the project's first third-party Go dependency (every prior module was stdlib-only, e.g. SPEC-0003 explicitly chose JSON over YAML "to avoid a third-party dependency for the repo's first Go module") - chosen over a stdlib-only JSON format or a hand-rolled parser after asking the user, since SPEC-0019's own example is written in YAML; `services/core/go.mod`/`go.sum` updated accordingly (`go get`/`go mod tidy`), the only module in the workspace with an external dependency so far. `container.go`'s `AgentRegistry` placeholder (SPEC-0020) was left untouched, for the same reason SPEC-0018 left it alone. `go build ./...`, `go vet ./...`, `go test ./... -v` clean in services/core (105 tests pass; 6 new: manifest load round-trip, three invalid-manifest cases [missing name, permission for an undeclared tool, malformed YAML], missing-file, agent-construction round-trip via `NewAgentFromManifest`, and constructor input-rejection [nil manifest, nil executor, invalid manifest]). Root `go build ./...`/`go test ./...` from the repo root fails with "pattern ./...: directory prefix . does not contain modules listed in go.work..."; reproduced even after `git stash`-ing back to a clean master, confirming this is a pre-existing environment quirk unrelated to this feature, not a regression - verified instead by running build/vet/test inside each of the 5 go.work module directories individually (all clean), same workaround the SPEC-0008 CRLF/gofmt note and the various `go test -race` unavailability notes above already established as acceptable for this environment. Built on feature/agent-manifest-system (off master, post SPEC-0018 merge).
  SPEC-0020   Completed         services/core/agent_registry.go: new `AgentRegistry` interface (`Register(Agent) error`, `Lookup(id string) (Agent, error)`, `Remove(id string) error`, `List() []Agent`) plus `Registry`, an in-memory, mutex-protected implementation (`NewRegistry()`) - fills the "Agent registration" / "Agent lookup" / "Agent removal" / "Agent listing" requirements of SPEC-0020, the second spec of Phase 4 Intelligence. `container.go`'s `AgentRegistry interface{}` placeholder (reserved by SPEC-0008) was replaced in-place by this real interface, mirroring `eventbus.go`'s SPEC-0009 precedent of the real `EventBus` interface living in its own file rather than as a `container.go` placeholder; `container_test.go`'s fake `agentRegistry` (previously a bare `struct{name string}`, valid only against the old empty-interface placeholder) is now a real `NewRegistry()`, since a bare struct no longer satisfies the real interface. `Register` validates the agent's SPEC-0018 `AgentMetadata` via its existing `Validate()` and rejects a duplicate ID with `packages/errors` `TypeAlreadyExists`/`AGENT_REGISTRY_DUPLICATE_AGENT` without overwriting the existing registration - satisfying "duplicate registrations are handled" by rejecting rather than silently replacing, chosen to match `Queue.Add`'s established convention for duplicate task IDs (`task_queue.go`, SPEC-0013). `Lookup`/`Remove` return `TypeNotFound`/`AGENT_REGISTRY_AGENT_NOT_FOUND` for an unknown ID; `List` returns agents sorted by ID for deterministic ordering. Deliberately did not add a separate "check capabilities" method: SPEC-0020's "Registry Usage" list (find by ID / check capabilities / route tasks) is satisfied by `Lookup` plus the returned `Agent`'s own `Metadata()`/`Execute()` (SPEC-0018) - `AgentMetadata` has no `Capabilities` field (SPEC-0019 already established Capabilities stays manifest-only, with no equivalent slot on that contract), so there is no separate capability concept for a registry-level method to expose. `packages/shared-types/agent.go`'s existing `Agent` struct (SPEC-0004, annotated for SPEC-0018/SPEC-0020) was considered as the registry-record shape but not used directly, since `services/core.Registry` stores the live SPEC-0018 `Agent` interface value itself (needed so `Lookup` callers can call `Execute`), not a separate static record - no gap this spec needs to close, just a different shape serving a different purpose. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (8 new tests: register+lookup, invalid-metadata rejection, discovery via Lookup/List, lookup-not-found, duplicate-rejection-does-not-overwrite, remove+double-remove, empty-list, and a 50-goroutine concurrent-registration test). `gofmt -l` clean on both new files (agent_registry.go, agent_registry_test.go); `container.go`/`container_test.go` are flagged by `gofmt -l` but `gofmt -d` confirms it's the same pre-existing CRLF/LF artifact noted since SPEC-0007/0008/0019, not a new issue. Root `go build ./...` still fails with the same pre-existing go.work resolution error noted since SPEC-0009. Built on feature/agent-registry (off master, post SPEC-0019 merge).
  SPEC-0021   Completed         services/core/agent_lifecycle.go: new `LifecycleManager` filling the "Agent initialization/startup/execution/shutdown/cleanup" requirements and 7-state machine (`REGISTERED`, `INITIALIZING`, `READY`, `RUNNING`, `STOPPING`, `STOPPED`, `FAILED`) of SPEC-0021, the third spec of Phase 4 Intelligence. `packages/shared-types/agent.go`'s `AgentStatus` (added under SPEC-0004 as a forward-declared placeholder explicitly annotated "SPEC-0021 Agent Lifecycle Manager", but only carrying 4 values - `idle`/`running`/`stopped`/`error` - that don't match this spec's actual 7-state list) was corrected to the real 7 states, mirroring `TaskStatus`'s precedent (SPEC-0004/SPEC-0012) of the shared-types package declaring the closed set of state values while `services/core` owns transition validation; the two existing `types_test.go` cases using the old `AgentStatusIdle` were updated to `AgentStatusRegistered`, the closest equivalent starting state. Also added `AgentTransition` (AgentID/From/To/Reason/Timestamp) mirroring `TaskTransition`, with an extra `Reason` field (omitted by every other transition) since, unlike `Task`, there's no persistent `*Agent` struct for a failure message to live on - `LifecycleManager` tracks state by agent ID rather than mutating an object, since `services/core.Agent` (SPEC-0018, the interface actually registered) and `types.Agent` (SPEC-0004, a separate DTO shape) are not the same type. `LifecycleManager` composes an `AgentRegistry` (SPEC-0020) rather than duplicating agent storage - `Register` delegates to it and only then records `REGISTERED`; `Initialize`/`Stop` call `Lookup` to get the live `Agent` for invoking two new optional capability interfaces, `AgentInitializer` (`Init(ctx) error`) and `AgentCleaner` (`Cleanup(ctx) error`) - a Go optional-interface pattern (mirroring `io.Closer`) chosen over changing the closed SPEC-0018 `Agent` contract itself, so an agent that needs no init/cleanup work implements neither and the corresponding phase is a no-op. A closed `agentValidTransitions` table plus per-agent `history` (`map[string][]types.AgentTransition]` under a `sync.Mutex`) directly mirrors `task_state_machine.go`'s `validTransitions`/`StateMachine.History` structure - `CanTransitionAgent`, `Register`, `Initialize`, `Start`, `Stop`, `Fail`, `State`, `History` are `StateMachine`'s `CanTransition`/`Transition`/`History` shape adapted to a by-ID rather than by-pointer model, since Agents (unlike Tasks) have no natural caller-owned struct to mutate. A hook (`Init`/`Cleanup`) failure re-uses the same `transition` call to move the agent to `FAILED` - already a valid destination from both `INITIALIZING` and `STOPPING` in the transition table - so no separate bypass path was needed. Deliberately did not add a `Container` slot (`container.go` has no pre-reserved `LifecycleManager` placeholder the way `AgentRegistry` had from SPEC-0008, and `TaskManager`'s slot has stayed an untouched placeholder through SPEC-0011-0017 despite that whole layer being built, establishing that Container wiring is added only when a spec pre-reserves the slot, not automatically for every new service type). `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (12 new tests: full REGISTERED->INITIALIZING->READY->RUNNING->STOPPING->STOPPED walk with history assertions [testing criterion 1], Cleanup-hook-invoked-on-Stop [criterion 2], Init-hook-failure and Cleanup-hook-failure both landing in FAILED with the wrapped error's code surfaced [criterion 3], explicit `Fail` recording a `Reason`, terminal-state re-transition rejection, not-registered lookups across every method, duplicate-registration passthrough from the underlying `AgentRegistry`, and history-slice copy-safety). `gofmt -l` clean on both new files; existing `agent.go`/`types_test.go` in `packages/shared-types` are flagged by `gofmt -l` but `gofmt -d` confirms the same pre-existing CRLF/LF artifact noted since SPEC-0007/0008/0019/0020, not a new issue. Root `go build ./...` still fails with the same pre-existing go.work resolution error noted since SPEC-0009. One pre-existing flaky test unrelated to this change was observed and left alone: `TestWorker_FailsTerminallyAfterMaxAttempts` (task_worker_test.go, SPEC-0015) fails intermittently (~1 in 5 runs) with a timing-dependent event-count assertion, reproducible on master before this branch's changes - out of scope for SPEC-0021, not touched. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed two Code Quality issues - `Initialize`/`Stop`'s hook-failure paths discarded the return value of their own follow-up transition to `FAILED`, silently masking a rare concurrent-state-change scenario where the recorded state could be left stuck at `INITIALIZING`/`STOPPING` instead of `FAILED`; both now explicitly capture it as best-effort (`_, _ = m.transition(...)`) with a comment. Also identified a narrow TOCTOU window in `Register` (the underlying `AgentRegistry` and the manager's own state map are updated under two separate locked sections) - accepted as a known limitation rather than fixed, since no concurrent caller exists yet and the codebase already tolerates similarly narrow races elsewhere (e.g. `EventBus`'s Publish/Close). Approved after fixes. `go build ./...`, `go vet ./...`, `go test ./... -v` re-verified clean after the fix. Built on feature/agent-lifecycle-manager (off master, post SPEC-0020 merge).
  SPEC-0022   Completed         services/core/agent_execution_loop.go: new `ExecutionLoop` filling the seven-stage cycle (Receive Task -> Analyze Context -> Create Plan -> Select Tools -> Execute Actions -> Evaluate Result -> Return Response) of SPEC-0022, the fourth spec of Phase 4 Intelligence, built directly on `Agent`/`Executor` (SPEC-0018/SPEC-0014) with no new dependency on any not-yet-built system. Four caller-supplied function types cover the stages no lower spec defines behavior for yet - there is still no LLM layer, Tool Registry, or Memory system: `ContextAnalyzer` (Analyze Context, optional, defaults to an empty map), `Planner` (Create Plan, required), `ToolCaller` (Execute Actions, required only if a `Plan` ever produces a `Step` naming a tool), and `ResultEvaluator` (Evaluate Result, optional, defaults to treating a non-nil `Step` error as the only failure). "Select Tools" is not a separate hook: a `Step.Tool` field the `Planner` sets when building the `Plan` is what `Execute Actions` reads, since tool selection is inseparable from planning which action a step performs - mirroring SPEC-0018's precedent that planning/tool-use stay internal to an Agent's own `Execute` rather than becoming separate contract surface. `ExecutionLoop.Run` has the exact `Executor`/`Agent.Execute` signature (`func(ctx, *types.Task) (map[string]any, error)`), so `loop.Run` passes directly to `NewWorker`/`NewAgentFromManifest` with no adapter, the same "no adapter needed" precedent SPEC-0018/SPEC-0019 already established. Failure handling is fail-fast (stops at the first Step whose evaluated outcome fails, matching `task_worker.go`'s existing `Executor`-failure precedent); every failure branch (cancellation, Analyze Context, Create Plan, or a Step) routes through one `fail` helper that wraps the cause with `packages/errors` `TypeInternal`/`EXECUTION_LOOP_*` (or `TypeCanceled`/`TypeTimeout`/`EXECUTION_LOOP_CANCELED` for a canceled `ctx`, mapped the same way `Worker.Stop` already maps `ctx.Err()`) plus `taskId`/`step`/`tool`/`stepIndex` context, logs it if a `Logger` is configured, and - critically for "failures return useful results" - `Run` always returns the accumulated `analysis`/`steps` response map alongside the error, not just on success. No `Container` slot was added (same reasoning as SPEC-0021: no pre-reserved placeholder exists for one). `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (12 new tests covering all three SPEC-0022 testing criteria - simple-task completion, tool execution with correct args/output capture, and failures returning useful/contextual results - plus plan/analysis-failure reporting, missing-`ToolCaller` handling, a `ResultEvaluator` rejecting an otherwise-successful step, an empty-`Plan` trivial success, context-cancellation stopping the loop before the next step, and a full `Worker`-driven integration test proving no adapter is needed). `gofmt -l` clean on both new files. Root `go build ./...` still fails with the same pre-existing go.work resolution error noted since SPEC-0009. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed two Code Quality gaps - failure logging originally covered only Step failures, leaving Analyze Context/Create Plan failures unobserved despite the same `Logger` being configured (fixed by routing every failure branch through the shared `fail` helper); and the loop never checked `ctx` cancellation between stages, so a canceled context could still let a multi-step `Plan` keep calling `ToolCaller` for every remaining Step rather than stopping proactively like `Worker.loop`'s existing `ctx.Done()` check between iterations (fixed by adding explicit cancellation checks before `Run` starts and before each Step, with a regression test). Accepted one known, out-of-scope limitation: `ExecutionLoop` does not check `AgentMetadata.Permissions` before invoking a `ToolCaller`, since no permission-checking system exists anywhere in the codebase yet (SPEC-0018/SPEC-0019 already established Tools/Permissions as bare identifiers a future permission system owns the meaning of) - enforcing permissions here would be new, unrequested scope ahead of that system existing. Approved after fixes; re-verified `go build`/`go vet`/`go test ./... -v` clean and `gofmt -l` clean. Built on feature/agent-execution-loop (off master, post SPEC-0021 merge).
  SPEC-0023   Completed         services/core/agent_context_builder.go: new `ContextBuilder` filling the "context before agent execution" requirements of SPEC-0023, the fifth spec of Phase 4 Intelligence, built directly on `Agent`/`AgentMetadata` (SPEC-0018), `types.Task` (SPEC-0011), and `HistoryRecord` (`task_history.go`, SPEC-0017) with no new dependency on any not-yet-built system. `ContextInput` carries the six listed inputs (UserMessage, ConversationHistory, Memories, Task, AvailableTools, PreviousResults); `Memories` and `AvailableTools` are caller-resolved `[]string` identifiers rather than fetched by the builder itself, mirroring `AgentMetadata.Tools`/`MemoryAccess`'s existing bare-string precedent, since the Memory layer (SPEC-0034 onward) and Tool Registry (SPEC-0043..0045) are both still Planned; `ConversationHistory` is likewise pre-rendered `[]string` turns rather than a typed chat-message shape, since no such type exists yet (Conversation Memory, SPEC-0036, is also Planned). `Build` assembles a `Context` (ordered `[]ContextItem` plus `TotalSize`/`Truncated`) in a fixed `contextSectionOrder` matching SPEC-0023's own Requirements listing - "maintain ordering" therefore has one canonical answer independent of the order a caller populates `ContextInput`'s fields in - and silently omits any section left empty or blank ("avoid unnecessary context"). "Support token limits" is a `SizeEstimator` (`func(ContextItem) int`, defaulting to word count) plus an optional `WithMaxSize` budget: items are added in section order until the next one would exceed it, at which point building stops and every section that lost items is recorded in `Truncated` - deliberately not full token accounting or "prioritize important information" logic, both of which are SPEC-0032 Context Window Manager's and SPEC-0033 Token Budget Manager's job, also still Planned; this builder only needs to not silently produce an unbounded context, not replace those specs. No `Container` slot was added (same reasoning as SPEC-0021/0022: no pre-reserved placeholder exists for one). Designed as a natural fit for `ExecutionLoop`'s `ContextAnalyzer` hook (SPEC-0022's Analyze Context stage) but not wired into it, since no spec has asked for that integration yet. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (10 new tests covering all three SPEC-0023 testing criteria - context generated correctly, required information included with correct ordering, and large contexts handled via size-limit truncation - plus empty-input/blank-entry omission, unlimited-by-default behavior, a custom `SizeEstimator`, truncation logging, and the `Task`/`HistoryRecord` integration boundaries). `gofmt -l` clean on both new files. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed one Code Quality gap - `Build` called the configured `SizeEstimator` unconditionally, so `WithSizeEstimator(nil)` or a zero-value `ContextBuilder{}` (built without `NewContextBuilder`) would panic on the first item; fixed by falling back to the default word-count estimator whenever none is configured, with a regression test. Accepted one known, out-of-scope limitation, consistent with SPEC-0022's own accepted gap: `ContextBuilder` does not check `AgentMetadata.Permissions` before including a tool identifier in `AvailableTools`, since permission filtering is the caller's responsibility to have already applied before populating `ContextInput` - no permission-checking system exists anywhere in the codebase yet. Approved after fix; re-verified `go build`/`go vet`/`go test ./... -v` clean and `gofmt -l` clean. Built on feature/agent-context-builder (off master, post SPEC-0022 merge).
  SPEC-0024   Completed         services/core/agent_permission.go: new `PermissionLevel`/`PermissionModel`/`PermissionChecker` filling the "security boundaries for agents" requirements of SPEC-0024, the sixth spec of Phase 4 Intelligence - directly closing the permission-checking gap SPEC-0022 and SPEC-0023 each explicitly called out and accepted as out-of-scope ("no permission-checking system exists anywhere in the codebase yet"). Distinct from SPEC-0019's existing per-manifest `Permissions map[string]ManifestPermission` (`require_confirmation` boolean, declared but never enforced): SPEC-0024 is a separate, centralized security policy table - `PermissionModel` (`map[string]AgentPermissions`, `AgentPermissions` a `map[string]PermissionLevel`) keyed by agent ID, loaded from YAML in the exact three-state shape (`allowed`/`approval_required`/`denied`) SPEC-0024's own example uses, via `LoadPermissionModel` (parses with the existing `gopkg.in/yaml.v3` dependency from SPEC-0019, then `Validate()`s every declared level). Category names (`filesystem`/`terminal`/`browser`/etc.) are plain identifiers, not a closed enum, mirroring `AgentMetadata.Tools`/`Permissions`' existing bare-string precedent (SPEC-0018) - matching `JARVIS_MASTER_ARCHITECTURE.md`'s Tool System responsibilities (filesystem access/terminal operations/browser automation/external integrations), which map onto SPEC-0024's four listed controls (Tool access/File access/Command execution/External communication). `PermissionModel.Level(agentID, category)` fails closed: an agent absent from the model, or present with no entry for that category, defaults to `PermissionDenied` rather than silently allowing an undeclared boundary. `PermissionChecker.Check(ctx, agentID, category)` is the enforcement point - `Allowed` returns nil immediately, `Denied` (including any fail-closed default) returns a `packages/errors` `TypePermissionDenied`/`AGENT_PERMISSION_DENIED` error (the `TypePermissionDenied` `Type` constant already existed in `packages/errors/type.go` since SPEC-0006 but had never been used until now), `ApprovalRequired` calls an optional `ApprovalFunc` and denies if none is configured, the func errors, or it declines - and every outcome (`allowed`/`approved`/`denied`/`denied_by_approver`/`denied_no_approver`/`approval_error`) is logged via an optional `packages/logger.Logger`, satisfying "permission checks are logged" without requiring a logger to be configured. `PermissionEnforcedToolCaller(checker, agentID, next ToolCaller)` wraps SPEC-0022's existing `ToolCaller` seam (`agent_execution_loop.go`'s Execute Actions stage) so enforcement sits exactly where every tool call already passes through - `ExecutionLoop`, `Agent`, `Manifest`, `Registry`, and `LifecycleManager` (SPEC-0018-0023) were left completely untouched, this spec is purely additive. `go build ./...`, `go vet ./...`, `go test ./... -v`, `gofmt -l` all clean across all 5 go.work modules (13 new top-level tests: SPEC-0024's three explicit testing criteria - restricted tools blocked, allowed tools execute, permission checks logged - plus YAML load/invalid-level/malformed/missing-file handling, fail-closed defaults for an absent agent and for an agent with zero declared categories, approval-required grant/decline/approver-error/no-approver paths, and a full integration test wiring `PermissionEnforcedToolCaller` into a real `ExecutionLoop.Run()` proving a denied step actually halts the loop rather than only working in isolation). `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted since SPEC-0005 - moot here regardless, since neither `PermissionModel` nor `PermissionChecker` mutate any field after construction. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): no blocking issues found; approved without changes. Accepted one known, out-of-scope limitation: nothing yet constructs a `PermissionChecker` at real agent startup or wires it into `Registry`/`LifecycleManager`, since no spec has defined where the permission YAML lives at runtime or when checking should be wired in system-wide - this delivers the enforcement primitive, ready for that future wiring. Built on feature/agent-permission-model (off master, post SPEC-0023 merge).
  SPEC-0025   Completed         services/core/agent_communication.go: new `AgentMessageKind`/`Communicator` filling the "communication between agents and the core runtime" requirements of SPEC-0025, the seventh and final spec of Phase 4 Intelligence's Agent layer - the first real consumer of `packages/shared-types/message.go`'s `Message` envelope (SPEC-0010), whose `MessageTypeAgentCommunication` constant had named this exact traffic since SPEC-0010 but had no builder or dispatcher until now. `AgentMessageKind` is a closed 5-value enum (`request`/`response`/`delegation`/`status_update`/`error_report`) recorded at `Message.Payload["kind"]`, covering exactly SPEC-0025's five listed message categories; `NewAgentRequest`/`NewDelegationMessage`/`NewStatusUpdateMessage`/`NewErrorReportMessage`/`NewAgentResponseMessage` are pure builders producing the envelope for each kind (`Destination` left empty for the two broadcast-style kinds, matching `Message.Destination`'s own SPEC-0010 doc comment). `Communicator` (wraps the SPEC-0020 `AgentRegistry`, and optionally the SPEC-0009 `EventBus` under a new `EventAgentMessage` type) is the routing/orchestration layer: `Request`/`Delegate` share one `dispatch` implementation that builds the outgoing Message, broadcasts a "started" status update, looks up and runs the destination Agent's `Execute` (SPEC-0018), validates the resulting `AgentResponse` via `ValidateAgentResponse` (must name its request/agent and carry exactly one of a successful outcome or a failure `Error`, satisfying "responses are validated"), broadcasts a "completed"/"failed" status update plus an error report on failure, and returns the response `Message` alongside the Agent's own execution error. `Delegate` additionally tags `task.Metadata["delegatedFrom"/"delegatedTo"]` before dispatch (SPEC-0025's own Core Agent -> Developer Agent example) but otherwise reuses `Request`'s exact code path - the destination Agent's `Execute` is the same seam either way. Deliberately stops at the Agent boundary rather than continuing to the "Tool Execution" step in SPEC-0025's own example diagram: the Tools layer (SPEC-0043 Tool Interface onward) is still `Planned`, not implemented, so `Delegate` does not assume a concrete tool-execution backend exists - flagged explicitly in `context/current-feature.md` at load time via the Dependency Graph's "Agents require: Tools" line. No `Container` slot was added (same reasoning as SPEC-0021-0024: no pre-reserved placeholder exists for one); `AgentRegistry`, `EventBus`, `Agent`, and `PermissionChecker` (SPEC-0009/0018/0020/0024) were all left completely untouched, this spec is purely additive. `go build ./...`, `go vet ./...`, `go test ./... -v`, `gofmt -l` all clean across all 5 go.work modules (15 new tests: SPEC-0025's three explicit testing criteria - agents can communicate, responses are validated, delegation works correctly - plus registry-lookup failure, Execute failure surfaced alongside a validated failure response, an always-invalid custom validator override, EventBus broadcast of status-update and error-report events, and table-driven `ValidateAgentResponse` coverage for every malformed/well-formed case). `go test -race` unavailable in this environment (no cgo toolchain), same constraint noted since SPEC-0005. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed one Code Quality gap before approval - `WithCommunicatorValidator(nil)` left `c.validate` nil, causing a nil-pointer panic in `dispatch` on the next `Request`/`Delegate` call, the same bug class SPEC-0023's `ContextBuilder` hit with `WithSizeEstimator(nil)`; fixed with the identical nil-fallback-at-point-of-use pattern, plus a regression test (`TestCommunicator_NilValidatorFallsBackToDefault`). Accepted one known, out-of-scope limitation: delegation is not gated by SPEC-0024's `PermissionChecker`, since that model governs an agent's tool-category access (filesystem/terminal/browser), not agent-to-agent delegation, and SPEC-0025's own Requirements don't ask for it. Approved after fix; re-verified `go build`/`go vet`/`go test ./... -v` clean and `gofmt -l` clean. Built on feature/agent-communication-protocol (off master, post SPEC-0024 merge).
  SPEC-0026   Completed         services/core/llm_provider.go: new `Provider` interface filling SPEC-0026's five listed requirements - the first spec of Phase 4 Intelligence's LLM branch, built directly on `packages/errors` with no dependency on the just-finished Agent layer (SPEC-0018-0025) or on any concrete model runtime, matching the Dependency Graph's "Agents -> {LLM, Memory, Tools, ...}" split into parallel branches. `Generate` (text responses), `Stream` (streaming responses, via an `onChunk func(StreamChunk) error` callback that halts the stream at its first returned error), `ListModels`/`ModelInfo` (model information), `HealthCheck`/`HealthStatus` (health checks), and `Configure`/`ProviderConfig` (provider configuration) cover the requirement list one-to-one; `GenerateRequest.Validate` mirrors `AgentMetadata.Validate`'s pattern (SPEC-0018) for the request shape `Generate` and `Stream` share. Deliberately interface-only: SPEC-0027 Ollama Integration, still `Planned`, owns the concrete HTTP client and config wiring, per that spec's own Overview ("Implement Ollama as the first local LLM provider") - `Provider.Name()`'s doc comment references "ollama" only as the value `packages/config`'s existing `ModelConfig.Provider` field already defaults to (reserved for SPEC-0026 since SPEC-0003), not as a hardcoded dependency, so Core Runtime stays decoupled from any single model implementation per ADR-0004. No `Container` slot was added (same reasoning as SPEC-0021-0025: no pre-reserved placeholder exists for one; a Provider slot is expected to land with SPEC-0027 or a later concrete-provider spec). `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (4 new tests: SPEC-0026's three explicit testing criteria - interface implementable, responses follow contract, failures handled correctly - via a `stubProvider`, plus table-driven `GenerateRequest.Validate` coverage). `gofmt -l` clean on both new files. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): no blocking issues found; approved without changes - no constructor/functional-options surface exists yet in this interface-only change, so the nil-callback-panic bug class the last two reviews (SPEC-0023, SPEC-0025) caught doesn't apply here. Built on feature/llm-provider-interface (off master, post SPEC-0025 merge).
  SPEC-0027   Completed         services/core/ollama_provider.go: new `OllamaProvider` filling SPEC-0027's five requirements - the first concrete implementation of the SPEC-0026 `Provider` interface, connecting Core Runtime to a real local model runtime per ADR-0004. `NewOllamaProvider()` defaults to `http://127.0.0.1:11434` with a 30s timeout and no default model; `Configure(ProviderConfig)` overrides BaseURL/Timeout and reads `Options["model"]` as the default model, covering SPEC-0027's "Ollama host URL / default model / timeout settings" Configuration section entirely through SPEC-0026's existing generic `ProviderConfig` shape - no new config type was needed. `Generate`/`Stream` both POST to `/api/generate` (JSON body, `Stream: true` toggling NDJSON), `Stream` scanning newline-delimited chunks and invoking `onChunk` per line, stopping immediately (and propagating the caller's error) if `onChunk` returns one - matching `Provider.Stream`'s documented contract from SPEC-0026. `ListModels` calls `/api/tags`; `HealthCheck` reuses the same endpoint, reporting `Healthy: false` (not an error) for both connection failures and non-2xx responses, since "is the server up" and "did my request fail" are different questions. All HTTP/network failures are wrapped via `packages/errors` into typed codes (`OLLAMA_CONNECTION_FAILED`/`TypeUnavailable`, `OLLAMA_REQUEST_TIMEOUT`/`TypeTimeout`, `OLLAMA_SERVER_ERROR`/`TypeUnavailable` for a 4xx/5xx body carrying an `error` field), and a request with no `Model` and no configured default model fails fast with `OLLAMA_MISSING_MODEL`/`TypeInvalidInput` before any network call. `container.go` gained the `Provider` slot SPEC-0026 anticipated but didn't add ("a Provider slot is expected to land with SPEC-0027") via a new `WithProvider(p Provider) ContainerOption`, following the exact `WithAgentRegistry` precedent (SPEC-0020); `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests were extended to cover it with a real `NewOllamaProvider()`, not a stub, since one now exists. No wiring from `packages/config`'s existing `ModelConfig.OllamaHost`/`OllamaPort` fields into a real `OllamaProvider.Configure` call was added, since no bootstrap/`main` entrypoint exists anywhere in the repo yet to own that wiring - consistent with `Container` itself never being constructed outside tests so far (SPEC-0008 onward). `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (17 new tests: SPEC-0027's four explicit testing criteria - connection succeeds via `HealthCheck`, models discovered via `ListModels`, streaming responses via NDJSON-decoding `Stream`, errors reported across Generate/Stream/ListModels/HealthCheck - plus `Configure` default/override/invalid-URL cases, missing-model rejection, server-error mapping, and stream-stops-on-callback-error, all via `httptest.NewServer` mocked Ollama responses rather than a real local server). Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed one Code Quality issue - `gofmt -l` flagged genuine (non-CRLF) struct-field misalignment in `ollamaGenerateResponse`/`ollamaModelEntry`/`ollamaModelDetails`/`OllamaProvider` plus missing trailing newlines in both new files, unlike every prior spec's `gofmt -l` flags on these two files which were the pre-existing CRLF/LF artifact; fixed via `gofmt -w`, re-verified `gofmt -l` clean and `go build`/`go vet`/`go test ./... -v` clean after. Approved after fix. Built on feature/ollama-integration (off master, post SPEC-0026 merge).
  SPEC-0028   Completed         packages/config/config.go and load.go: extends SPEC-0003's `ModelConfig` with `Models map[string]Model` (named model profiles - `general`/`coding`, matching SPEC-0028's own YAML example verbatim, defaulting to `qwen`/`qwen-coder`), `DefaultModel` (default model selection), and `AgentModels map[string]string` (agent-specific model overrides), plus a new `ModelConfig.ModelFor(agentName string) (Model, error)` method resolving an agent's override key if set, else falling back to `DefaultModel`, erroring if the resolved key has no `Models` entry - covering SPEC-0028's "Default model selection" and "Agent-specific models" requirements directly. `Model` (Provider/Name/Temperature/MaxTokens/Options) covers "Temperature settings", "Token limits" (`MaxTokens` - deliberately not `ContextSize`, since context-window sizing is SPEC-0032/SPEC-0033's job and both are still `Planned`), and "Runtime parameters" (`Options map[string]any`, a free-form catch-all not yet consumed by any provider). `load.go`'s existing `validate()` gained the checks needed for SPEC-0028's third testing criterion ("Invalid configuration is rejected"): empty or non-`ollama` provider on any named model (ADR-0004: Ollama is the only supported runtime), empty model name, temperature outside [0, 2], negative `MaxTokens`, `DefaultModel` unset while `Models` is non-empty, an unresolvable `DefaultModel`, and any `AgentModels` value with no matching `Models` entry - so `Load` fails closed with a descriptive error instead of silently accepting bad model config or deferring the failure to a later `ModelFor` call. Built entirely on the SPEC-0003 `Load`/`Defaults`/`validate` pattern already in `packages/config`, with no new dependency and no reach into `services/core`'s SPEC-0026 `Provider` interface or SPEC-0027 `OllamaProvider` - resolving a `Model` into an actual provider call is left to SPEC-0029 Model Router, which this spec unblocks. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (9 new tests plus an extended `TestLoad_Defaults`: SPEC-0028's three explicit testing criteria - config loads, agent overrides work via `TestModelConfig_ModelFor`/`TestModelConfig_ModelFor_UnknownKeyFails`/`TestLoad_AgentModelOverrideAppliesFromFile`, invalid configuration rejected via seven `TestLoad_ValidationRejects*` cases). `gofmt -l` clean on both changed files. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): review pass found and fixed one real Code Quality gap - a `models` block with an empty `defaultModel` passed `validate()` and would only fail later at `ModelFor()` call time - by adding the missing check and a regression test; re-verified `gofmt -l` and `go build`/`go vet`/`go test ./...` clean after fix. Approved after fix. Built on feature/model-configuration-system (off master, post SPEC-0027 merge).
  SPEC-0029   Completed         services/core/model_router.go: new `ModelRouter` filling SPEC-0029's "intelligent model selection based on task requirements" requirement - the third spec of Phase 4 Intelligence's LLM branch, sitting directly on top of `Provider` (SPEC-0026), `OllamaProvider` (SPEC-0027), and `ModelConfig` (SPEC-0028) without modifying any of the three. `Route(ctx, RouteRequest)` resolves a `cfgpkg.Model` via the four listed "Routing Inputs" in priority order - `RouteRequest.UserPreference` (an explicit `Models` key) first, then `ModelConfig.AgentModels[AgentType]` (SPEC-0028's existing agent-override map, reused rather than duplicated), then a caller-supplied `taskModels map[string]string` (Task.Type -> `Models` key, e.g. `"coding" -> "coding"`, covering SPEC-0029's own coding/conversation/reasoning examples), then `ModelConfig.DefaultModel` - any input naming a key absent from `Models` (a dangling `AgentModels`/task-type entry, or an unrecognized `UserPreference`) is treated as no match and falls through rather than erroring, only reporting `MODEL_ROUTER_NO_MODEL`/`TypeNotFound` if nothing at all resolves. `taskModels` is deliberately router-side, caller-supplied state rather than a new `ModelConfig` field, since SPEC-0028's schema only defines agent-specific overrides, not task-type ones, and extending that schema would be a config-schema change outside this spec's own scope. "Model availability" is checked against `Provider.ListModels` (SPEC-0026/0027): if the resolved model's `Name` is absent, `Route` falls back to `DefaultModel` (when that names a different, configured model) and marks `RouteDecision.Fallback = true`, satisfying "fallback models work"; a `ListModels` failure is deliberately *not* treated as unavailability (kept the original decision) so a temporarily-unreachable Ollama server can't force every request onto the default model. Every `Route` call logs the decision (task/agent/preference inputs, resolved key/model/reason, fallback flag) via an optional `packages/logger.Logger`, satisfying "routing decisions are logged" without requiring one to be configured. `ModelRouter` holds no state that mutates after construction (`cfg`/`provider`/`log`/`taskModels` are all set once in `NewModelRouter`), so `Route` is safe for concurrent use without a mutex. A review pass caught one architecture-fit gap and fixed it before completion: `Container` (SPEC-0008) has an established pattern of gaining a real slot for every LLM-branch service as its own spec completes (`Provider` got `Container.Provider` + `WithProvider` as part of SPEC-0026/0027) - `ModelRouter` had skipped that step, so `container.go` gained `Router *ModelRouter` + `WithRouter(r *ModelRouter) ContainerOption` (doc comment updated to cite SPEC-0029), with `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests extended to cover it. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (17 new tests across 9 functions in `model_router_test.go`: SPEC-0029's three explicit testing criteria - correct models selected across the full priority order [6 subtests], fallback models work, routing decisions are logged - plus dangling-override fall-through [2 subtests], no-resolvable-model error, available-model-not-falled-back-from, default-model-itself-unavailable staying `Fallback: false`, a `ListModels` failure not forcing a fallback, and a 50-goroutine concurrent-`Route` test; plus 2 extended `container_test.go` cases for the new `Router` slot). `gofmt -l` clean on both new files; `container.go`/`container_test.go` are flagged by `gofmt -l` but `gofmt -d` confirms the same pre-existing CRLF/LF artifact noted since SPEC-0007/0008/etc., not a new issue. One pre-existing flaky test unrelated to this change was observed once and not reproduced on rerun: `TestWorker_FailsTerminallyAfterMaxAttempts` (task_worker_test.go, SPEC-0015/SPEC-0021's same known flake) - out of scope, not touched. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed the `Container` wiring gap above; no other issues - error handling consistent with `packages/errors` conventions, no security concerns (routing inputs are only ever used as map keys), no scope creep, ADR-0004 (Ollama-only) untouched. Approved after fix; re-verified `go build`/`go vet`/`go test ./... -v` clean and `gofmt -l` clean. Built on feature/model-router (off master, post SPEC-0028 merge).
  SPEC-0030   Completed         services/core/stream_handler.go: new `StreamHandler` filling SPEC-0030's four requirements - the fourth spec of Phase 4 Intelligence's LLM branch, sitting directly on top of `Provider.Stream` (SPEC-0026/0027) without modifying `Provider`, `OllamaProvider`, or `ModelRouter` (SPEC-0029). `Stream(ctx, GenerateRequest, onEvent func(StreamEvent) error) (StreamResult, error)` invokes `onEvent` once per `StreamChunk` the Provider produces (`StreamEvent.Chunk`, the "Token streaming" requirement) alongside the full response text accumulated so far (`StreamEvent.Partial`, the "Partial responses" requirement), so a caller doesn't have to accumulate chunks itself. `StreamHandler` deliberately does not call `ModelRouter.Route` - it takes a `GenerateRequest` with `Model` already resolved, keeping routing (SPEC-0029) and streaming (SPEC-0030) as separate, caller-composed concerns, consistent with `Provider.Generate`/`Provider.Stream` (SPEC-0026) not calling the router either. "Stream cancellation": `Stream` checks `ctx.Err()` before starting and after every chunk, independent of whether the underlying Provider itself observes `ctx` - if cancelled, it returns a `packages/errors` `TypeCanceled`/`STREAM_CANCELLED` error and marks `StreamResult.Cancelled`. "Error recovery": `StreamResult.Text` always holds whatever text had been accumulated before a failure or cancellation rather than discarding it, and `StreamHandler` holds no state across calls, so it's safe to call again immediately after a failure. Every call logs its outcome (model, cancelled, accumulated text length - deliberately not the text itself, to avoid leaking conversation content into structured logs) via an optional `packages/logger.Logger`. `Container` (SPEC-0008) gained the same real-slot treatment every LLM-branch service has gotten as its spec completes (`Provider` via SPEC-0026/0027, `Router` via SPEC-0029): `StreamHandler *StreamHandler` + `WithStreamHandler(h *StreamHandler) ContainerOption` in `container.go`, with `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests extended to cover it. A review pass found and fixed one real Code Quality bug before completion: cancellation was originally detected only via `errors.Is(err, errors.TypeCanceled)` on the error `Provider.Stream` returned, which relied on `StreamHandler`'s own per-chunk check producing that error first - but `OllamaProvider.mapConnectionError` classifies a cancelled in-flight HTTP request as `TypeUnavailable`/`OLLAMA_CONNECTION_FAILED` (`url.Error.Timeout()` is `false` for `context.Canceled`), so a real race where Ollama's own network layer notices cancellation first would have misreported a genuine cancellation as a plain failure. Fixed by checking `ctx.Err()` directly instead of the error's `Type`, and normalizing any resulting error to `TypeCanceled` (wrapping the original as its cause) so a caller gets a reliable signal regardless of which Provider produced the failure; regression test `TestStreamHandler_CancellationDetectedEvenWhenProviderMisclassifiesError` uses a `raceCancelProvider` stub mirroring Ollama's real misclassification. `go build ./...`, `go vet ./...`, `go test ./... -v` clean across all 5 go.work modules (9 new tests in `stream_handler_test.go`: SPEC-0030's three explicit testing criteria - tokens arrive progressively, streams can be cancelled [including the already-cancelled-context and provider-misclassified-error cases], failures recover correctly - plus partial-response accumulation, onEvent-error propagation, and outcome logging; 10x rerun clean; `-race` unavailable in this environment, no cgo/gcc configured). `gofmt -l` clean on both new files after one real alignment fix (`gofmt -w`); `container.go`/`container_test.go` are flagged by `gofmt -l` but `gofmt -d` confirms the same pre-existing CRLF/LF artifact noted since SPEC-0007/0008/0029, not a new issue. One pre-existing flaky test unrelated to this change was observed once in a full-suite run and passed 5/5 in isolated reruns: `TestWorker_FailsTerminallyAfterMaxAttempts` (task_worker_test.go, SPEC-0015/SPEC-0021/SPEC-0029's same known flake) - out of scope, not touched. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed the cancellation-detection gap above; no other issues - error handling consistent with `packages/errors` conventions, no sensitive data logged, no scope creep, ADR-0004 (Ollama-only) untouched. Approved after fix; re-verified `go build`/`go vet`/`go test ./... -v` clean and `gofmt -l` clean. Built on feature/streaming-response-handler (off master, post SPEC-0029 merge).
  SPEC-0031   Completed         services/core/prompt_template.go: new `PromptTemplate`/`PromptRegistry` filling SPEC-0031's four requirements - the fifth spec of Phase 4 Intelligence's LLM branch, sitting alongside `Provider`/`GenerateRequest` (SPEC-0026/0027) and `StreamHandler` (SPEC-0030) without modifying either: a `PromptTemplate.Render` produces a plain string a caller assigns to `GenerateRequest.Prompt` itself, rather than either spec calling into the other. "System prompts" and "Agent instructions" are both just a `PromptTemplate` distinguished by a `Kind` field (`PromptKindSystem`/`PromptKindInstructions`) - metadata only, `Render` behaves identically regardless. "Dynamic variables" is `PromptVariables` (`UserContext`, `TaskInformation`, `Memories`, `AvailableTools`, `Instructions`, plus a free-form `Extra map[string]string`), rendered into a `text/template` `Body`; `VariablesFromContext(Context) PromptVariables` bridges SPEC-0023's `ContextBuilder` output for the spec's own example variables, mapping `UserContext` to the combined `ContextSectionUserMessage`+`ContextSectionConversationHistory` sections and `TaskInformation`/`Memories`/`AvailableTools` to their same-named `ContextBuilder` sections; `PreviousResults` has no named variable in SPEC-0031's example list and is deliberately left unmapped - a caller who needs it reads it from the `Context` directly rather than PromptVariables gaining a field the spec never asked for. "Prompt versions" is `PromptRegistry`: it keeps every registered `(Name, Version)` pair rather than overwriting on re-registration (`Register` returns a `packages/errors` `TypeAlreadyExists` error on a duplicate version), with `Get(name, version)`, `Latest(name)`, and `Versions(name)` (sorted ascending) to retrieve them - the "Versions can be tracked" testing criterion. `Container` (SPEC-0008) gained the same real-slot treatment every LLM-branch service has gotten as its spec completes: `PromptRegistry *PromptRegistry` + `WithPromptRegistry(r *PromptRegistry) ContainerOption` in `container.go`, with `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests extended to cover it. A review pass against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing), run after the initial merge, found and fixed one real Code Quality gap: `Render`'s doc comment credited `Option("missingkey=error")` with catching a `Body` referencing an undefined `PromptVariables` *field* (e.g. `{{.Typo}}`) - verified with a throwaway script that this is wrong, since `text/template` always errors on an undefined struct field regardless of that option. `missingkey=error`'s actual, and only, effect here is on `Extra`, a `map[string]string`: without it, `{{.Extra.missingKey}}` would silently render `<no value>` instead of failing. Fixed the comment and added `TestPromptTemplate_Render_UnknownExtraKeyFails` as a regression test isolating that specific behavior. No other issues found: architecture fit confirmed (no upward dependency violations, doesn't call into or get called by `Provider`/`StreamHandler`), error context never includes `Body`/rendered content, `PromptRegistry` correctly `RWMutex`-guarded. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (24 tests in `prompt_template_test.go` covering SPEC-0031's three explicit testing criteria - prompts render correctly, variables inject correctly [including `Extra` map entries, the `VariablesFromContext` bridge in both populated and empty-`Context` cases, and the `missingkey=error`-guarded unknown-`Extra`-key case specifically], versions can be tracked [register/get/duplicate-rejected/latest/versions-sorted/unknown-name-or-version/independent-names-do-not-collide] - plus `PromptTemplate.Validate` field coverage and malformed/unknown-struct-field template rejection; new tests clean, 3x full-workspace rerun clean). `gofmt -l` flags both new files, matching the pre-existing CRLF/LF artifact noted since SPEC-0007/0008/0029/0030 (`gofmt -d` shows no actual diff) - not a new issue. One pre-existing flaky test unrelated to this change, previously documented against SPEC-0015/0021/0029/0030, was observed once during this review's full-workspace reruns and passed 5/5 in isolation: `TestWorker_FailsTerminallyAfterMaxAttempts` (task_worker_test.go) - out of scope, not touched. Approved after fix. Built on feature/prompt-template-system (off master, post SPEC-0030 merge); review fix committed directly to master.
  SPEC-0032   Completed         services/core/context_window_manager.go: new `WindowManager` filling SPEC-0032's three "Must" requirements - the sixth spec of Phase 4 Intelligence's LLM branch, and the real token-accounting/prioritization layer `agent_context_builder.go`'s own package doc comment explicitly deferred to this spec, sitting directly on top of `ContextBuilder`/`Context`/`ContextItem` (SPEC-0023) without modifying any of them. `Fit(Context, budget int) (Context, Usage)` takes an already-built `Context` and a token budget and returns the highest-`ContextPriority` subset that fits, reusing `Context`'s own shape (`Items`/`TotalSize`/`Truncated`) so existing callers like `VariablesFromContext` (SPEC-0031) keep working unmodified against its output. "Prioritize important information" is `ContextPriority` (`Low`/`Normal`/`High`/`Critical` - named `ContextPriority` rather than `Priority` to avoid colliding with `task_queue.go`'s existing `TaskPriority` constants, SPEC-0013, which share the same vocabulary for an unrelated concept) plus a `PriorityFunc` defaulting to ranking `UserMessage`/`Task` critical, `Memories` high, `ConversationHistory`/`AvailableTools` normal, `PreviousResults` low; within a tier, later (more recently added) items are kept over earlier ones, e.g. the newest conversation turns over the oldest. "Remove unnecessary context" is `Fit`'s greedy fill: items are considered in priority-then-recency order and kept while they still fit the budget, skipping (not aborting on) any that don't, so a single oversized item can't starve smaller lower-priority ones of remaining budget. "Track token usage" is `Usage{Budget, Used, BySection}`, returned alongside the trimmed `Context` on every call. `TokenEstimator` defaults to a ~4-characters-per-token heuristic (`defaultTokenEstimator`) - a stand-in for a real model-specific tokenizer, exactly as `agent_context_builder.go`'s own word-count `defaultSizeEstimator` was for SPEC-0023, since `Provider` (SPEC-0026) has no `CountTokens`/tokenize method yet and ADR-0004's local-only Ollama runtime has no tokenize-without-generating endpoint to call. `Fit` deliberately does not resolve what a model's actual token budget should be: `packages/config`'s `Model.MaxTokens` (SPEC-0028) caps generated *output* length, not input context size, and SPEC-0028's own build history reserved resolving real context-window sizing for SPEC-0032/SPEC-0033 - owning that resolution (model lookup, defaults, per-agent overrides) is left to SPEC-0033 Token Budget Manager, still Planned, so `Fit` only takes a caller-supplied `budget int` and has nothing to gain from reaching into config itself. `Container` (SPEC-0008) gained the same real-slot treatment every LLM-branch service gets as its spec completes: `WindowManager *WindowManager` + `WithWindowManager(w *WindowManager) ContainerOption` in `container.go`, with `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests extended to cover it. A review pass against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing), run before merge, found and fixed one real Code Quality bug: `Fit`'s trimming branch rebuilt `Truncated` from scratch instead of merging with the incoming `Context.Truncated`, silently losing record of any section an upstream `ContextBuilder` had already dropped entirely before the `Context` ever reached `Fit` (such a section has zero items left in `c.Items` for `Fit` itself to notice and record as dropped) - verified with a throwaway test first, then fixed by seeding `droppedSections` from `c.Truncated` before the kept/dropped scan, plus `TestWindowManager_Fit_PreservesUpstreamTruncation` as a permanent regression test. No other issues found: architecture fit confirmed (no upward dependency violations, no new third-party dependency, doesn't reach into `packages/config` or call into `Provider`), `Fit` never errors matching `ContextBuilder.Build`'s same "fewer items than requested is still valid" precedent, nil `TokenEstimator`/`PriorityFunc` (whether via `WithTokenEstimator(nil)`/`WithPriorityFunc(nil)` or a zero-value `WindowManager{}`) fall back to defaults rather than panicking, mirroring the nil-callback-panic bug class the SPEC-0023/0025 reviews previously caught. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (SPEC-0032's three explicit testing criteria - context stays within limits, important information remains, large conversations are handled - plus token-usage-tracking, custom `TokenEstimator`/`PriorityFunc` overrides, nil-option fallback, trim logging, and the upstream-`Truncated`-preservation regression case). `gofmt -l` clean on both new files; `container.go`/`container_test.go` are flagged by `gofmt -l` but `gofmt -d` confirms the same pre-existing CRLF/LF artifact noted since SPEC-0007/0008/etc. Approved after fix. Built on feature/context-window-manager (off master, post SPEC-0031 merge).
  SPEC-0033   Completed         services/core/token_budget_manager.go: new `BudgetManager` filling SPEC-0033's "Track"/"Support" requirement groups - the seventh and final spec of Phase 4 Intelligence's LLM branch, sitting directly on top of `ModelConfig.ModelFor` (SPEC-0028), `Provider.ListModels`'s `ModelInfo.ContextSize` (SPEC-0026/0027), and `WindowManager.Fit` (SPEC-0032) without modifying any of them - the exact resolution `WindowManager`'s own SPEC-0032 build note explicitly deferred to this spec ("owning that resolution (model lookup, defaults, per-agent overrides) is SPEC-0033 Token Budget Manager's job"). `Limit(ctx, agentName)` resolves an agent's configured `Model` via `ModelConfig.ModelFor`, then that model's real context window size via the configured `Provider`'s `ListModels` (matching by `Model.Name`, requiring a positive `ContextSize`), falling back to a configurable default (`defaultContextLimit`, 4096) when no `Provider` is set or it reports no match or fails - mirroring `ModelRouter.isAvailable`'s SPEC-0029 precedent of not guessing at an unreachable provider's state. "Track Input tokens/Output tokens/Context usage" is `Record(ctx, agentName, TokenUsage{InputTokens, OutputTokens})`, which accumulates per-agent cumulative usage under a mutex, resolves the current `Limit`, and returns a `BudgetReport{Model, Usage, Limit, Status}`; `Report`/`Reset` read/clear that per-agent state without re-resolving anything. "Track Model limits" is `Limit` itself. "Token estimation" is `EstimateTokens`, a thin wrapper reusing SPEC-0032's own `TokenEstimator` type rather than redefining one. "Budget warnings" is `BudgetStatus` (`BudgetOK`/`BudgetWarning`/`BudgetExceeded`, classified via `classifyBudget` against a configurable `warnAt` threshold defaulting to 80%), logged via an optional `packages/logger.Logger` whenever `Record`'s resulting Status is not `BudgetOK` - deliberately logging only counts/model/status, never prompt content, mirroring `StreamHandler`'s SPEC-0030 same choice. "Context reduction strategies" is `ReduceContext(ctx, agentName, Context)`, a thin composition of `Limit` with a configured `WindowManager.Fit` (SPEC-0032) so a caller doesn't have to resolve a budget itself before trimming. `Container` (SPEC-0008) gained the same real-slot treatment every LLM-branch service has gotten as its spec completes: `BudgetManager *BudgetManager` + `WithBudgetManager(m *BudgetManager) ContainerOption` in `container.go`, with `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests extended to cover it. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (SPEC-0033's three explicit testing criteria - token usage is tracked, limits trigger correctly [BudgetOK->BudgetWarning->BudgetExceeded transitions plus a custom `WithWarnThreshold`], reduction strategies execute - plus per-agent usage isolation, `Reset`, `Limit`'s full fallback chain [provider ContextSize match/no-match/ListModels-failure/no-provider], nil-option fallback for both `TokenEstimator` and `WindowManager`, and unresolvable-model error paths). `gofmt -l` clean on both new files after one genuine (non-CRLF) comment-alignment fix via `gofmt -w`; `container.go`/`container_test.go` are flagged by `gofmt -l` but `gofmt -d` confirms the same pre-existing CRLF/LF artifact noted since SPEC-0007/0008/etc., not a new issue. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed two issues before approval - (1) Code Quality: `resolveModel` propagated `ModelConfig.ModelFor`'s bare `fmt.Errorf` unchanged, the only services/core component that would hand a caller an error carrying no `packages/errors` `Type`; fixed by wrapping it via `errors.Wrap(err, TypeNotFound, "BUDGET_MANAGER_MODEL_UNRESOLVED", ...)`, matching `ModelRouter`'s `MODEL_ROUTER_NO_MODEL` precedent (SPEC-0029), plus a regression test asserting `errors.Is(err, errors.TypeNotFound)`. (2) Testing: `TestBudgetManager_Record_LogsWarningOnlyWhenNotOK` asserted only that `Record` didn't error, never actually inspecting log output despite its name; fixed using the same `logger.New("test", logger.WithOutput(&buf))` buffer-assertion pattern `TestWindowManager_Fit_LogsTrimming` (SPEC-0032) already established, plus a new `TestBudgetManager_Record_NoLoggerRunsSilently` test covering the nil-logger path the original test's name had claimed to cover instead. No other issues found: architecture fit confirmed, no sensitive data logged, no scope creep, `ModelConfig`/`Provider`/`WindowManager`/`ModelRouter`/`StreamHandler`/`PromptRegistry` (SPEC-0026-0032) all left completely untouched beyond the additive `Container` slot. Approved after fixes; re-verified `go build`/`go vet`/`go test ./...` clean and `gofmt -l` clean after both fixes. Built on feature/token-budget-manager (off master, post SPEC-0032 merge).
  SPEC-0034   Completed         services/core/memory_interface.go: new `Memory` interface filling SPEC-0034's five listed operations (Store/Retrieve/Search/Update/Delete) plus the four required memory types (`MemoryTypeConversation`/`MemoryTypeUserProfile`/`MemoryTypeKnowledge`/`MemoryTypeExperience`) - the first spec of the Memory layer, built directly on the just-completed Agent (SPEC-0018-0025) and LLM (SPEC-0026-0033) layers per `JARVIS_DEPENDENCY_GRAPH.md`'s core flow, with no other prerequisite spec. Interface only, no concrete storage engine: SPEC-0035 (Memory Storage Abstraction, still Planned) owns the actual persistence, matching how SPEC-0026 defined `Provider` with no concrete provider and SPEC-0018 defined `Agent` with no concrete agent - same reasoning, no `Container` slot added (SPEC-0026's own build note: "No Container slot added, same reasoning as SPEC-0021-0024"). `MemoryRecord` (ID/Type/Content/Metadata/CreatedAt/UpdatedAt) is the stored unit; `MemoryRecord.Validate()` enforces a known `Type` and non-empty `Content`, mirroring `GenerateRequest.Validate()`'s (SPEC-0026) per-field-error precedent. `MemoryQuery` (Type/Query/Limit/Filters) is `Search`'s input, with its own `Validate()` requiring a non-empty `Query` and, if set, a known `Type`; `Type` is optional on `MemoryQuery` (empty means all types) but required on `MemoryRecord` (a stored record must know what kind of memory it is). `ADR-0007` (relational + vector storage split) is deliberately not reflected in the interface itself - `Memory` names no backend, same as `Provider` naming no LLM vendor despite `ADR-0004` locking Ollama. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (11 new tests covering SPEC-0034's three testing criteria - interface implementable via a `stubMemory`, operations follow contract across all four memory types including Search-by-Type filtering and Update/Delete round-trips, and failures are handled correctly including `TypeNotFound` on unknown IDs for Retrieve/Update/Delete - plus table-driven `MemoryRecord.Validate`/`MemoryQuery.Validate` coverage for valid/missing-Type/invalid-Type/missing-Content/missing-Query cases). Built on feature/memory-interface (off master, post SPEC-0033 merge).
  SPEC-0035   Completed         services/core/memory_storage.go: new `MemoryStorageProvider` interface (Name/Put/Get/Query/Replace/Remove) - the storage-engine-level contract a concrete backend implements, sitting below SPEC-0034's `Memory` and never referenced outside this abstraction layer - plus `StorageMemory`, the `Memory` implementation that routes each call by `MemoryRecord.Type`/`MemoryQuery.Type` to the `MemoryStorageProvider` configured for it (`WithProviderFor`), falling back to a default provider otherwise. Callers depend only on `Memory`; which provider (or providers) actually serve which memory type is invisible to them - the spec's "hide implementation details from agents" requirement. `Store`'s returned ID encodes the resolved provider's `Name()` plus its provider-local ID (e.g. `local::3`) via `idSeparator`, so `Retrieve`/`Update`/`Delete` can route back to the right provider from the ID alone with no separate index to keep in sync; `splitID` returns a `TypeNotFound` error for a malformed or unrecognized-provider ID. Two concrete providers ship with this spec, matching ADR-0007's relational+vector split and satisfying "Local database storage"/"Vector storage"/"Future storage providers" without introducing a real database or vector-DB dependency this phase doesn't call for (no such dependency exists anywhere in the codebase yet - `task_history.go`, SPEC-0017, is the closest precedent, also a plain mutex-guarded map): `memory_storage_local.go`'s `LocalStore` (case-insensitive substring `Query` match, an in-memory stand-in for a relational `LIKE` query) and `memory_storage_vector.go`'s `VectorStore` (naive word-overlap-scored, descending-ranked `Query`, an explicitly-labeled placeholder for the real embedding similarity SPEC-0038 Vector Memory Engine/SPEC-0039 Embedding Pipeline own - not a redesign of either, still Planned). Both are plain `sync.Mutex`-guarded maps with a monotonically incrementing local ID counter, mirroring `HistoryStore`'s existing concurrency pattern. `Container` (SPEC-0008) gained the real-slot treatment its own SPEC-0034 build note deferred ("no Container slot added... a slot is expected to land with SPEC-0035 or a later concrete-implementation spec", mirroring `Provider`'s SPEC-0026-interface/SPEC-0027-concrete split): `Memory Memory` + `WithMemory(m Memory) ContainerOption` in `container.go`, with `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests extended to cover it. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (24 new tests across `memory_storage_test.go`/`memory_storage_local_test.go`/`memory_storage_vector_test.go` covering SPEC-0035's three testing criteria - storage providers can be swapped [same `Memory` contract driven identically regardless of backing provider], data contracts remain consistent [`MemoryRecord` round-trips Type/Content/Metadata through `Store`/`Retrieve` across differently-routed types], storage errors are handled [invalid record rejected before reaching any provider, malformed/unknown-provider/unknown-local-ID cases all surface `TypeNotFound`, no-provider-configured-for-type case] - plus per-type routing, cross-provider `Search` merging when `Query.Type` is unset vs. scoped to one provider when set, and each concrete provider's own Put/Get/Query/Replace/Remove/Name coverage including substring-match and word-overlap-ranking specifics). A review pass against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing) found and fixed one Architecture gap before completion: the initial implementation skipped the `Container` slot on the mistaken belief that `StorageMemory` had no nil-safe default, when in fact every other optional `Container` slot (`Provider`, `Router`, `StreamHandler`, etc.) is exactly as unconstructible-standalone yet still defaults to nil until wired via its `With*` option - fixed as described above, with a regression test (`TestNewContainer_OptionsWireStubSlots`/`TestNewContainer_UnwiredSlotsDefaultToNil` extended). No other issues found: architecture fit confirmed (no upward dependency violations, `MemoryStorageProvider` never leaks outside this file and its two provider implementations), no sensitive data logged (no logging added, none required by the spec), no scope creep, `Memory`/`MemoryRecord`/`MemoryQuery` (SPEC-0034) left completely untouched beyond the additive `Container` slot. Approved after fix; re-verified `go build`/`go vet`/`go test ./...` clean and `gofmt -l` clean. `gofmt -l` clean on all seven new/changed files. Built on feature/memory-storage-abstraction (off master, post SPEC-0034 merge).
  SPEC-0036   Completed         services/core/conversation_memory.go: new `ConversationMessage`/`ConversationMemory` filling SPEC-0036's requirements (store conversations/messages/roles/timestamps/metadata/related tasks; support loading recent conversations, searching previous conversations, context preparation) - the third spec of the Memory branch of Phase 4 Intelligence, built as a façade over SPEC-0034's `Memory` interface rather than a new storage layer, since Conversation is already one of `MemoryType`'s four required kinds. `MessageRole` (user/assistant/system) and `ConversationMessage` (ID/ConversationID/Role/Content/RelatedTaskIDs/Metadata/CreatedAt) are new types with a `Validate()` mirroring `MemoryRecord.Validate()`'s per-field-error precedent (SPEC-0034). `AddMessage` stores each message as a `MemoryRecord{Type: MemoryTypeConversation}`, encoding conversationID/role/relatedTaskIDs into `MemoryRecord.Metadata`, then re-`Retrieve`s it so the returned message carries the provider-assigned `CreatedAt` rather than a client-guessed one. Since `Memory.Search` requires a non-empty query and has no notion of "distinct conversations", `ConversationMemory` keeps its own in-process index (conversationID -> ordered message IDs, plus a recency-ordered conversation list and a `lastMessageAt` map) alongside the underlying `Memory` - the same map+mutex approach `memory_storage_local.go`'s `LocalStore` already uses to satisfy its own interface - so `Conversation` (chronological retrieval), `RecentConversations` (most-recently-active-first summaries), and `PrepareContext` (trailing-N-message window for an agent's context) don't depend on `Memory.Search` at all; `Search` itself still delegates to `Memory.Search` scoped to `MemoryTypeConversation` for free-text lookup. No changes to `memory_interface.go`/`memory_storage.go`/`memory_storage_local.go` (SPEC-0034/0035 contracts) or `container.go` - `ConversationMemory` wraps whatever `Memory` a caller already has (e.g. `container.Memory`), matching how SPEC-0023's `ContextBuilder` already takes conversation history as a caller-resolved input rather than fetching it itself. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (15 new tests covering SPEC-0036's three testing criteria - messages persist [round-tripped through the underlying `Memory` with role/content/metadata/related-tasks intact], conversations can be retrieved [chronological order, isolated per conversation], context loading works [`PrepareContext`'s trailing-window and no-cap cases] - plus per-field `ConversationMessage.Validate()` cases, recency-ordering and limit behavior for `RecentConversations`, and `Search` correctly scoping to `MemoryTypeConversation` only). `gofmt -l` clean on both new files. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed one Code Quality gap - `ConversationSummary.LastMessageAt` was declared and documented but never populated by `RecentConversations` (always the zero time); fixed by threading a `lastMessageAt` index through `touchIndex`, set from `AddMessage`'s provider-assigned `CreatedAt`, with a regression test. Also fixed a gofmt misalignment in the new metadata-key const block. No other issues found: architecture fit confirmed (no upward dependency violations, no SPEC-0034/0035 contract changes, no new `Container` slot needed), no sensitive data logged, no scope creep. Approved after fix; re-verified `go build`/`go vet`/`go test ./...` clean and `gofmt -l` clean. Built on feature/conversation-memory (off master, post SPEC-0035 merge).
  SPEC-0037   Completed         services/core/user_profile_memory.go: new `ProfileCategory`/`ProfileFact`/`UserProfileMemory` filling SPEC-0037's requirements (store user preferences, personal information, working style, projects, and other important facts; verified by facts being stored, retrieved, and updates replacing outdated information) - the fourth spec of the Memory branch of Phase 4 Intelligence, built as a façade over SPEC-0034's `Memory` interface rather than a new storage layer, since `MemoryTypeUserProfile` is already one of `MemoryType`'s four required kinds (defined in SPEC-0034, unused by any concrete feature until now). `ProfileCategory` (preference/personal_info/working_style/project/fact, matching the spec's five listed categories) and `ProfileFact` (ID/Key/Category/Content/Metadata/CreatedAt/UpdatedAt) are new types with a `Validate()` mirroring `MemoryRecord.Validate()`'s per-field-error precedent (SPEC-0034). Unlike `ConversationMemory` (SPEC-0036), which only ever appends, `ProfileFact` carries a caller-assigned `Key` identifying what it's about (e.g. `preference:language`); `Remember` stores each fact as a `MemoryRecord{Type: MemoryTypeUserProfile}` and keeps an in-process `Key -> record ID` index (the same map+mutex approach `ConversationMemory` and `memory_storage_local.go`'s `LocalStore` already use) so a second `Remember` under an already-known `Key` calls `Memory.Update` in place instead of `Memory.Store`-ing a duplicate, satisfying "updates replace outdated information" without any free-text matching. `Fact(key)` retrieves a single remembered fact by `Key`, returning `TypeNotFound` if nothing is remembered under it; `Facts(ctx)` lists every remembered fact ordered by `Key`, for assembling a user profile summary into an agent's context. No changes to `memory_interface.go`/`memory_storage.go`/`memory_storage_local.go`/`conversation_memory.go` (SPEC-0034/0035/0036 contracts) or `container.go` - `UserProfileMemory` wraps whatever `Memory` a caller already has, matching `ConversationMemory`'s own precedent. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (17 tests covering SPEC-0037's three testing criteria - facts can be stored, facts can be retrieved [including the unknown-key `TypeNotFound` case], updates replace outdated information [same ID and `CreatedAt` preserved, no duplicate record left behind] - plus per-field `ProfileFact.Validate()` cases, `Facts()` key-ordering, and `ProfileCategory.IsValid()`). `gofmt -l` clean on both new files. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): found and fixed one Code Quality bug - `Remember`'s check-then-act on `byKey` (does this Key already exist?) released the mutex before acting on the answer, deciding `Store` vs. `Update` outside the lock, so two concurrent `Remember` calls on the same new Key could both see "not found," both `Store`, and permanently orphan whichever record lost the final index write; fixed by holding the mutex across the whole read-decide-write sequence, with a regression test (`TestUserProfileMemory_Remember_ConcurrentSameKeyConverges`). No other issues found: architecture fit confirmed (no upward dependency violations, no SPEC-0034/0035/0036 contract changes, no new `Container` slot needed, same as `ConversationMemory`), no sensitive data logged, no scope creep. Approved after fix; re-verified `go build`/`go vet`/`go test ./...` clean and `gofmt -l` clean. Built on feature/user-profile-memory (off master, post SPEC-0036 merge).
  SPEC-0038   Completed         services/core/memory_storage_vector.go (upgraded) + new services/core/memory_embedding.go: fills SPEC-0038's four requirements (embedding storage, similarity search, metadata filtering, ranking results) - the fifth spec of the Memory branch of Phase 4 Intelligence, replacing `VectorStore`'s SPEC-0035 placeholder (naive word-overlap-count `Query`, explicitly labeled in that spec's own build note as this spec's job to replace) with real embedding-based cosine similarity, rather than introducing a new storage layer or touching SPEC-0034's `Memory`/SPEC-0035's `MemoryStorageProvider` contracts. `Embedder` (memory_embedding.go) is a new one-method interface (`Embed(text string) []float64`); `HashEmbedder` is its dependency-free default implementation (the hashing trick: each word hashed via `fnv.New32a` into one of `Dims` buckets, default 256, accumulating a term-frequency vector), deliberately not a real model-backed embedding - `SPEC-0039` (Embedding Pipeline, still Planned) is expected to supply a model-backed `Embedder` behind the same interface, mirroring the `Provider`-interface/`Ollama`-concrete split SPEC-0026/0027 already established. `cosineSimilarity(a, b []float64) float64` is the shared similarity primitive (0 for any zero-magnitude vector, never NaN). `VectorStore` gained an `embeddings map[string][]float64` alongside its existing `records` map (embedded via the configured `Embedder` - `WithEmbedder` option, defaulting to `HashEmbedder` - on both `Put` and `Replace`, removed together with the record on `Remove`, all still under the same single `sync.Mutex` as before - no new locking introduced); `Query` now scores each type/filter-matching candidate by `cosineSimilarity` against the query's own embedding instead of raw shared-word count, excluding non-positive scores and ranking descending (`CreatedAt`-ascending tie-break unchanged). "Metadata filtering" is `matchesFilters`, new equal-match filtering of `q.Filters` against `rec.Metadata` via `reflect.DeepEqual` (safe against uncomparable dynamic types, unlike `==`) - a gap `VectorStore.Query` had left entirely unimplemented since SPEC-0035, now exercised both directly and through the `Memory`/`StorageMemory` interface boundary. No changes to `memory_interface.go`/`memory_storage.go`/`memory_storage_local.go`/`conversation_memory.go`/`user_profile_memory.go` (SPEC-0034/0035/0036/0037 contracts) or `container.go` - `NewVectorStore()` remains callable with no arguments, and no new `Container` slot was needed (the existing `Memory` slot already covers it). No new third-party dependency (`go.mod`/`go.sum` unchanged, `gopkg.in/yaml.v3` remains the only one). `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (25 memory/vector-related tests covering SPEC-0038's three testing criteria - memories are embedded [`HashEmbedder` determinism, configured/default `Dims`, empty-text zero vector, distinct content -> distinct vectors], similar memories are retrieved [cosine-similarity ranking, including a case proving it's real similarity and not raw overlap count: an exact match outranks a noisy superset match that shares the same word count], filtering works [metadata equal-match, nil-Metadata edge case, and end-to-end propagation through `StorageMemory.Search`] - plus `Replace` re-embedding and `Remove`-of-missing-ID error-handling parity with `LocalStore`). `-race` unavailable in this environment (no cgo toolchain), same constraint noted since SPEC-0005/0007/0009. `gofmt -l` clean on new/changed files aside from the pre-existing CRLF/LF artifact noted since SPEC-0007/0008/etc. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): no issues found - architecture fit confirmed (no upward dependency violations, ADR-0007's vector-storage-for-semantic-retrieval choice followed, `VectorStore` stays in-memory consistent with `LocalStore`'s own "no real DB dependency this phase" precedent since neither this spec nor any ADR calls for a concrete external vector DB yet), embeddings/`records` maps mutated only under the existing single mutex (no race risk added), no sensitive data logged, no scope creep. Built on feature/vector-memory-engine (off master, post SPEC-0037 merge).
  SPEC-0039   Completed         new services/core/embedding_pipeline.go + services/core/ollama_embedder.go: fills SPEC-0039's four requirements (text chunking, embedding generation, metadata attachment, storage submission) across its four required sources (conversations, documents, notes, code) - the sixth spec of the Memory branch of Phase 4 Intelligence, and the spec SPEC-0038's own build note named as the one expected to supply a real model-backed `Embedder` behind its `Embed(text string) []float64` interface. `ollama_embedder.go`'s `OllamaEmbedder` is that model-backed `Embedder`, calling a local Ollama server's `POST /api/embeddings` (mirroring `OllamaProvider`'s SPEC-0027 HTTP-client pattern) and dropping in wherever an `Embedder` is accepted (`VectorStore.WithEmbedder`, this spec's own `WithPipelineEmbedder`) with zero changes to `VectorStore` itself; since `Embed`'s signature has no error return (locked by SPEC-0038), any connection failure/non-2xx/malformed-body/server-reported-error degrades to a `nil` vector rather than panicking - `cosineSimilarity` already treats a zero-magnitude vector as similarity 0, so a failed call just never matches instead of corrupting results. `embedding_pipeline.go` is the pipeline proper: `SourceType` (conversation/document/note/code, matching SPEC-0039's four sources) with `memoryType()` mapping conversation to SPEC-0034's `MemoryTypeConversation` and document/note/code to `MemoryTypeKnowledge` (the one SPEC-0034 `MemoryType` that fits general ingested content and was otherwise unused since SPEC-0034 shipped); `EmbeddingInput`/`Validate()` mirrors `MemoryRecord.Validate()`'s per-field-error precedent; `Chunker` interface + `FixedSizeChunker` (dependency-free default, word-count windows with configurable overlap) is the "text chunking" requirement, structured the same way SPEC-0038 paired `Embedder` with the dependency-free `HashEmbedder`; `EmbeddingPipeline.Process` chunks content, embeds each chunk via its configured `Embedder` (default `HashEmbedder`, override via `WithPipelineEmbedder` - named distinctly from `VectorStore`'s own `WithEmbedder` option type to avoid a same-package identifier collision), attaches `source`/`chunkIndex`/`chunkCount`/`embedding` onto `MemoryRecord.Metadata` alongside the caller's own metadata, and calls `Memory.Store` per chunk - the embedding computed here is independent of whatever a downstream `MemoryStorageProvider` like `VectorStore` recomputes internally for its own indexing, by design, so the vector is available to callers regardless of which storage engine backs `Memory`. A content string that Validates (non-empty) but chunks to zero pieces (e.g. whitespace-only) surfaces an explicit `EMBEDDING_INPUT_NO_CHUNKS` error rather than silently returning no records. `services/core/container.go` gained an `EmbeddingPipeline *EmbeddingPipeline` slot + `WithEmbeddingPipeline` option, matching the precedent SPEC-0035 set when `Memory` gained its own slot (a new top-level capability other components, e.g. the still-Planned SPEC-0040 Knowledge Ingestion Pipeline, will depend on) - `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests extended to cover it. No changes to `memory_interface.go`/`memory_storage.go`/`memory_storage_vector.go`/`memory_embedding.go`/`conversation_memory.go`/`user_profile_memory.go` (SPEC-0034/0035/0036/0037/0038 contracts). No new third-party dependency (`go.mod`/`go.sum` unchanged, `gopkg.in/yaml.v3` remains the only one). `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (30 new tests covering SPEC-0039's three testing criteria - text is processed [`FixedSizeChunker` window/overlap/empty/shorter-than-size behavior, multi-chunk content actually split and each chunk independently submitted], embeddings are generated [non-empty vectors, distinct chunk content producing distinct vectors, the pipeline's own computed vector round-tripping through `Memory.Store`/`Retrieve` via the `embedding` metadata key], metadata is preserved [caller-supplied metadata surviving alongside `source`/`chunkIndex`/`chunkCount` end-to-end through `Memory.Store`+`Retrieve`] - plus all four `SourceType`-to-`MemoryType` mappings, `EmbeddingInput.Validate()` per-field cases, `Memory.Store` error propagation, and `OllamaEmbedder.Embed`'s success/HTTP-error/server-reported-error/malformed-response/unreachable-server/no-model-configured paths via `httptest`). `gofmt -l` clean on all four new/changed files. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): no issues found - architecture fit confirmed (no upward dependency violations, ADR-0004's Ollama-only/no-external-API-dependency choice followed by `OllamaEmbedder`, no locked interface/contract changed), no sensitive data logged, no scope creep. Built on feature/embedding-pipeline (off master, post SPEC-0038 merge).
  SPEC-0040   Completed         new services/core/knowledge_ingestion.go: fills SPEC-0040's five requirements (ingest Markdown files, PDFs, text files, code repositories, documentation) via the Input/Parser half of its own pipeline diagram (Input -> Parser -> Chunking -> Embedding -> Storage) - the seventh and final spec of the Memory branch of Phase 4 Intelligence, sitting directly on top of SPEC-0039's `EmbeddingPipeline`, which already implements Chunking/Embedding/Storage and was the slot SPEC-0039's own build note reserved this spec for. `IngestFormat` (markdown/pdf/text/code) plus `DetectFormat` (extension-based, an explicit allowlist of code extensions for `code` rather than a blacklist, since a repository tree also contains binaries/images that must never be read as UTF-8 text) route to one of two `Parser` implementations: `PlainTextParser` (markdown/text/code - already plain text, passed through unchanged) and `PDFParser`, which extracts text via a new third-party dependency, `github.com/ledongthuc/pdf` - a pure-Go, no-cgo PDF reader, chosen to keep PDF ingestion local-first per ADR-0008 (no external service/process) - the project's second third-party Go dependency after `gopkg.in/yaml.v3` (SPEC-0019). `KnowledgeIngestionPipeline.IngestFile` reads one file, detects its format, parses it, attaches `path`/`filename`/`format` metadata (plus any parser-derived metadata, e.g. `PDFParser`'s `pageCount`) alongside SPEC-0039's own `source`/`chunkIndex`/`chunkCount` metadata, and calls `EmbeddingPipeline.Process`; `IngestDirectory` walks a tree via `filepath.WalkDir` for the "code repositories"/"documentation" requirements, silently skipping any file `DetectFormat` doesn't recognize and never descending into `.git`/`node_modules`/`vendor`/`.venv`/`__pycache__`, failing fast (propagating the first error) rather than continuing past a bad file. `services/core/container.go` gained a `KnowledgeIngestionPipeline *KnowledgeIngestionPipeline` slot + `WithKnowledgeIngestionPipeline` option, matching the precedent SPEC-0039 set when `EmbeddingPipeline` gained its own slot; `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests extended to cover it. No changes to SPEC-0034/0035/0038/0039 contracts (`memory_interface.go`/`memory_storage.go`/`memory_storage_vector.go`/`embedding_pipeline.go`/`ollama_embedder.go`). `services/core/go.mod`/`go.sum` gained `github.com/ledongthuc/pdf`; the module's `go` directive was bumped 1.23 -> 1.24.1, that dependency's own minimum (`go.work`'s directive bumped to match). `go build .`/`go vet .`/`go test .` clean for package `core` (20 new tests in `knowledge_ingestion_test.go`, including a hand-built minimal in-memory PDF fixture rather than a binary testdata file, covering SPEC-0040's three testing criteria - documents ingest successfully, content is searchable, metadata remains attached - across all five required source types, plus format detection, parser defaults/overrides via the new `WithParser` option, unsupported-format/missing-file/empty-content error paths, and directory-walk skip-dir/skip-unsupported-file/error-propagation behavior; `container_test.go` extended for the new slot). Verification was scoped to package `core` non-recursively (`go build .`, not `./...`) rather than the full workspace script, since an unrelated in-progress subpackage the user was concurrently developing in the same working tree, `services/core/voice` (SPEC-0053/0054/0055, importing `github.com/gen2brain/malgo`), does not yet build on its own - confirmed pre-existing and untouched by this change; `packages/shared-types`'s `TestAgent_EmptyCapabilitiesOmitted` similarly fails on that same concurrent, unrelated work and was left alone. One pre-existing flaky test unrelated to this change, already noted under SPEC-0021: `TestWorker_FailsTerminallyAfterMaxAttempts` (task_worker_test.go, timing-based, fails intermittently even in isolation). `gofmt -l` clean on the two new files; `container.go`/`container_test.go`/`go.mod`/`go.sum` are flagged by `gofmt -l` but this is the same pre-existing CRLF/LF artifact noted since SPEC-0007/0008/etc., not a new issue. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): no blocking issues found - architecture fit confirmed (no upward dependency violations, ADR-0008's local-first/open-source choice followed by the new PDF dependency, no locked interface/contract changed, scope stayed within `services/core` and its dependency manifests); noted two non-blocking observations for future hardening rather than fixes - `IngestFile` takes a raw filesystem path with no sandboxing (acceptable now since it's a library primitive, not yet exposed to untrusted input; path/permission sandboxing is SPEC-0049 Filesystem Tool's job, still Planned) and no size/resource limits on PDF decompression or file reads (matches existing codebase posture, e.g. `FixedSizeChunker` also has no input cap, acceptable under ADR-0002's local-first/single-user threat model). Approved without changes. Built on feature/knowledge-ingestion-pipeline (off master, post SPEC-0039 merge).
  SPEC-0041   Completed         new services/core/memory_retrieval.go: fills SPEC-0041's four "Retrieve based on" inputs (user query, task context, agent role, importance) plus its ranking/filtering/context-limit requirements - the eighth spec of the Memory branch of Phase 4 Intelligence, built directly on top of SPEC-0034's `Memory`/SPEC-0035's `StorageMemory`/SPEC-0038's embedding-backed `VectorStore` with no changes to any of those three contracts. `MemoryRetriever` wraps a `Memory` and composes one search string per `RetrievalRequest` from `Query` (required) + `TaskContext` + `AgentRole` (`searchText()`, blank parts omitted), submitted to `Memory.Search` uncapped (`MemoryQuery.Limit` left 0) so reranking always sees every relevance-ordered candidate before any cutting happens. `Memory`/`MemoryRecord` (SPEC-0034) name no importance field, and SPEC-0042 (Memory Consolidation Engine, the spec that would own computing importance over time) is still Planned; rather than widen the locked SPEC-0034 contract or block on SPEC-0042, `MemoryRetriever` reads importance from the existing free-form `MemoryRecord.Metadata` under an `"importance"` key (the same convention `knowledge_ingestion.go` already uses for parser-derived fields), treating an absent or non-float64 value as neutral (`1.0`) - `RetrievalRequest.MinImportance` filters below that value. Ranking blends relevance with importance rather than introducing a second, disconnected sort: since `Memory.Search`'s result order is itself the only relevance signal exposed across the `Memory` interface boundary (SPEC-0034 intentionally returns no raw similarity score), `rankByRelevanceAndImportance` scores position `i` as `1/(i+1) * importance` and stable-sorts descending, so an above-neutral-importance record can outrank a slightly-more-relevant one without a low-relevance record ever beating a highly relevant one outright; `RetrievalRequest.Limit` (0 = uncapped) then trims the already-ranked result, and `RetrievalRequest.Filters` passes through unchanged to the existing `MemoryQuery.Filters` exact-match mechanism `VectorStore.Query` already implements. `services/core/container.go` gained a `MemoryRetriever *MemoryRetriever` slot + `WithMemoryRetriever` option, matching the precedent SPEC-0039/SPEC-0040 set when `EmbeddingPipeline`/`KnowledgeIngestionPipeline` gained their own slots. No changes to `memory_interface.go`/`memory_storage.go`/`memory_storage_vector.go`/`memory_embedding.go`/`embedding_pipeline.go`/`ollama_embedder.go`/`knowledge_ingestion.go` (SPEC-0034/0035/0038/0039/0040 contracts). No new third-party dependency. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (9 new tests covering SPEC-0041's three testing criteria - relevant memories are returned, irrelevant memories are filtered [zero-similarity records excluded through the real `StorageMemory`+`VectorStore` path], retrieval respects limits [both an isolated-`Memory`-stub unit test and an end-to-end test against real `VectorStore` ranking] - plus a stub-`Memory` unit test proving `RetrievalRequest`'s search-text composition/`Type`/`Filters` pass through correctly and `Search` is always called uncapped, importance-outranks-relevance-order and `MinImportance`-filtering cases, and `RetrievalRequest.Validate()`'s missing-Query/invalid-Type error paths). `gofmt -l` flags `container.go` alongside the same pre-existing CRLF/LF artifact noted since SPEC-0007/0008/etc., not a new issue; the two new files are themselves `gofmt -l` clean. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): no issues found - architecture fit confirmed (no upward dependency violations, both prerequisite specs SPEC-0038-0040 Completed before starting, no locked interface/contract changed), no sensitive data logged, no scope creep. Approved without changes. Built on feature/memory-retrieval-system (off master, post SPEC-0040 merge).
  SPEC-0042   Completed         new services/core/memory_consolidation.go: fills SPEC-0042's four requirements (importance scoring, duplicate detection, memory updates, expiration handling) - the ninth and final spec of the Memory branch of Phase 4 Intelligence, and the spec SPEC-0041's own build note named as the one that would own computing importance over time; `MemoryRetriever` (SPEC-0041) already read `MemoryRecord.Metadata["importance"]` but nothing wrote it until now. `ConsolidationEngine` operates on a caller-supplied batch of `MemoryRecord`s (Memory/SPEC-0034 has no "list all" query - `MemoryQuery.Query` must be non-empty - so Consolidate cannot fetch its own working set) rather than querying `Memory` itself: `ImportanceScorer`/`DefaultImportanceScorer` score each record (a `pinned` metadata override, or a content-length-based score capped at `maxScoredImportance`); `groupDuplicates` clusters same-`MemoryType` records whose content embeddings (reusing SPEC-0038's `Embedder`/`cosineSimilarity`, defaulting to `HashEmbedder`) are at or above `DuplicateThreshold`, picking the highest-importance member (ties broken by earliest `CreatedAt`) as survivor - a deliberate single-linkage-from-anchor simplification over full transitive clustering, documented in code; every duplicate is deleted via `Memory.Delete` (`ActionMerged`), and the survivor's importance gets a `reinforcementPerDuplicate` bonus per merge (repetition is itself a signal something matters). Survivors/singletons at or above `MinImportance` have their scored importance (plus a `consolidatedCount` running total) persisted via `Memory.Update` (`ActionKept`); those below `MinImportance` are deleted via `Memory.Delete` once older than `ExpireAfter` (`ActionExpired`) or otherwise left untouched (`ActionIgnored`) - gating expiration on importance rather than age alone is what keeps an old-but-important (e.g. pinned) record from ever expiring, satisfying the "important memories are preserved" testing criterion even under SPEC-0042's own "expiration handling" requirement. `services/core/container.go` gained a `ConsolidationEngine *ConsolidationEngine` slot + `WithConsolidationEngine` option, matching the precedent SPEC-0039/0040/0041 set; `container_test.go`'s unwired-slots-default-to-nil and options-wire-stub-slots tests extended to cover it. No changes to `memory_interface.go`/`memory_storage.go`/`memory_storage_vector.go`/`memory_storage_local.go`/`memory_embedding.go`/`embedding_pipeline.go`/`memory_retrieval.go` (SPEC-0034/0035/0038/0039/0041 contracts) - `ConsolidationEngine` reuses their existing `Memory`/`Embedder`/`cosineSimilarity` surfaces unchanged. No new third-party dependency. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (17 new tests covering SPEC-0042's three testing criteria - important memories are preserved [pinned + old record survives, importance persisted], duplicates are merged [same-type near-identical content merges to one survivor with an accumulated `consolidatedCount`, different-`MemoryType` near-identical content does not merge], low-value memories are ignored [short/trivial content left untouched while fresh, deleted once past `ExpireAfter`] - plus an injected-clock expiration test, `DefaultImportanceScorer`'s pinned-override/length-growth/cap behavior, direct coverage of every `With*` option (`WithImportanceScorer`, `WithMinImportance`, `WithDuplicateThreshold`, `WithConsolidationEmbedder`, added during review to match the option-coverage precedent `embedding_pipeline_test.go` set), a missing-ID input-validation error path, and an end-to-end run against a real `StorageMemory`+`VectorStore`). `gofmt -l` flags the two files this spec modified (`container.go`, `container_test.go`) for the same pre-existing CRLF/LF artifact noted since SPEC-0007/0008/etc. (this Windows checkout's `core.autocrlf=true`, not part of `go_all.ps1`'s gate) - confirmed every already-committed `services/core/*.go` file (e.g. `memory_retrieval.go`, `memory_interface.go`, `knowledge_ingestion.go`) is flagged the same way, so this is not a regression; the two brand-new files were written LF-only, the opposite inconsistency, equally harmless. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): one gap found and fixed before completion - the option-coverage tests listed above were missing from the initial pass and were added during review, no other issues found; architecture fit confirmed (no upward dependency violations, all five resolved prerequisite specs SPEC-0034/0035/0038/0039/0041 Completed before starting, no locked interface/contract changed, ADR-0007's relational+vector storage split respected with no new storage backend introduced), no sensitive data logged, no scope creep. Built on feature/memory-consolidation-engine (off master, post SPEC-0041 merge).
  SPEC-0043   Completed         new services/core/tool.go: fills SPEC-0043's Tool Interface contract (Tool ID/Name/Description/InputSchema/OutputSchema/Permissions/Execution handler/error handling) - the first spec of the Tools branch of Phase 4 Intelligence, built directly on the completed Agent layer (SPEC-0018..0025), with SPEC-0018's own build note ("Tool Registry SPEC-0043..0045") naming this exact contract as the not-yet-built system meant to give `AgentMetadata.Tools`/`.Permissions` real meaning. `ToolMetadata` (ID/Name/Description/InputSchema/OutputSchema/Permissions) mirrors `AgentMetadata`'s shape exactly (`agent.go`, SPEC-0018) so the runtime can discover/validate either layer symmetrically; `ToolMetadata.Validate()` enforces the same minimum identity (non-empty ID/Name) via the same `packages/errors` `TypeInvalidInput` pattern. `Schema`/`SchemaField` (Name/Type/Required) is a deliberately minimal ordered-fields shape for the Input/Output schema requirements - `Schema.Validate` checks only required-field presence, not deep type/range/format checking (left to SPEC-0044's manifest system and beyond); an empty `Schema` is a valid free-form contract, matching the loose `map[string]any` the existing `ToolCaller` seam (`agent_execution_loop.go`, SPEC-0022) already uses. `Tool.Execute(ctx, input) (map[string]any, error)` deliberately drops the tool-name argument `ToolCaller` carries (a Tool already knows its own identity via `Metadata()`), so wiring a `Tool` into that seam needs only the same small, name-dropping wrapper - no real adapter - that `Agent.Execute`/`ExecutionLoop.Run` already established against `Executor`; proven directly by an integration test driving a real `ExecutionLoop` through a `Tool`'s `Execute` via exactly that wrapper. `Permissions` is a `[]string` of bare capability identifiers (e.g. "filesystem.read"), mirroring `AgentMetadata.Permissions`'s existing precedent - descriptive only, aligned with but not enforced by this contract; actual allow/approval/deny enforcement stays SPEC-0024's `PermissionChecker`'s job, not duplicated here. `ValidateToolInput(tool, input)` is a convenience helper a Tool's own `Execute` can call to satisfy the "Validate inputs" responsibility in one line. No `Container` slot was added (same reasoning as SPEC-0021-0025: no pre-reserved placeholder exists for one); `services/tools` remains an empty `.gitkeep` scaffold, matching the precedent that `agents/*` stayed empty while Agent-layer logic lived in `services/core`. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (10 new tests covering all three SPEC-0043 testing criteria - interface implementable, metadata validates correctly [valid/missing-ID/missing-Name table], execution follows contract [success/failure/cancellation] - plus `ValidateToolInput` satisfied/missing-required/nil-valued-required cases, empty/optional-only `Schema.Validate` edge cases, and an end-to-end integration test proving a `Tool` drives a real `ExecutionLoop` via its `ToolCaller` seam with only a name-dropping wrapper). `gofmt -l` clean on both new files (one real misalignment - a struct literal field, not the pre-existing CRLF/LF artifact noted since SPEC-0007/0008/etc. - found and fixed during review). Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): architecture fit confirmed (no upward dependency violations, both prerequisite specs SPEC-0018/SPEC-0024 Completed before starting, no locked interface/contract changed, scope stayed within `services/core/tool.go`+`tool_test.go` with no premature SPEC-0044/0045 manifest/registry work), no sensitive data logged, no scope creep. Approved after the gofmt fix. Built on feature/tool-interface (off master, post SPEC-0042 merge).
  SPEC-0044   Completed         new services/core/tool_manifest.go: fills SPEC-0044's Tool Manifest System requirements (Identity, Description, Capabilities, Input requirements, Permissions, Configuration) - the second spec of the Tools branch of Phase 4 Intelligence, built directly on `Tool`/`ToolMetadata` (`tool.go`, SPEC-0043, Completed), which had itself named this exact manifest system as its intended producer. Mirrors SPEC-0019's `Manifest`/`NewAgentFromManifest` pattern (`agent_manifest.go`) exactly, on the Tool side: `ToolManifest` (Name/Description/Capabilities/Input/Permissions/Config, YAML-tagged) is loaded and validated by `LoadToolManifest` (distinct function name from the existing agent-side `LoadManifest` in the same `core` package); `Validate()` requires a non-empty Name (Identity) and a non-empty Name on every declared `ManifestInputField`. `Metadata()` derives a `ToolMetadata` from the manifest - Name doubles as ID (matching `agent_manifest.go`'s `Manifest.Metadata` precedent), Permissions maps straight across (a flat `[]string`, unlike the agent manifest's per-tool `map[string]ManifestPermission` - SPEC-0044's own example shows a flat list, and there is no second list to cross-reference permissions against the way agent manifests cross-reference `Tools`), and `Input` converts to a `core.Schema` of `SchemaField`s; `Capabilities` and `Config` stay manifest-only, since `ToolMetadata`'s contract - like `AgentMetadata`'s - carries only identity and declared requirements, not this richer manifest-only detail (the same choice `agent_manifest.go` made for `Capabilities`/`Model`/`Config`). `NewToolFromManifest(m, execute ToolExecutor)` builds a concrete `Tool` (`manifestTool`) from a validated manifest plus a caller-supplied `ToolExecutor` (a named type matching `Tool.Execute`'s signature exactly, so no adapter is needed) - satisfying "tools can be created from manifests" the same way `NewAgentFromManifest` satisfies the analogous agent criterion, since a YAML manifest cannot itself carry executable behavior. All-new error codes are prefixed `TOOL_MANIFEST_*` (`TOOL_MANIFEST_READ_FAILED`/`_PARSE_ERROR`/`_MISSING_NAME`/`_INPUT_MISSING_NAME`/`_NIL`/`_MISSING_EXECUTOR`), deliberately distinct from `agent_manifest.go`'s `MANIFEST_*` codes so `errors.HasCode` checks in the shared `core` package stay unambiguous between the two manifest systems. No `Container` slot was added (same reasoning as SPEC-0021-0025/0043: no pre-reserved placeholder exists for one); `services/tools` remains an empty `.gitkeep` scaffold, matching SPEC-0043's own precedent. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (6 new tests covering all three SPEC-0044 testing criteria - manifests load correctly, invalid manifests fail [missing name, input field missing name, malformed YAML], tools can be created from manifests - plus a missing-file distinct-error-code case, a minimal-manifest edge case reproducing SPEC-0044's own literal example [only name+permissions set, proving an empty `InputSchema` accepts any/nil input], and nil-manifest/nil-executor/invalid-manifest rejection). One pre-existing flaky test unrelated to this change, already noted under SPEC-0021/0040: `TestWorker_FailsTerminallyAfterMaxAttempts` (task_worker_test.go, timing-based, fails intermittently even in isolation) - reproduced on the master baseline via `git stash` to confirm it is not caused by this work. `gofmt -l` flags both new files for the same pre-existing LF-only-vs-CRLF-checkout artifact noted since SPEC-0007/0008/etc. (this Windows checkout's `core.autocrlf=true`) - `gofmt -d` shows a full-file line-ending-only diff, no real formatting issue, matching the precedent SPEC-0042's brand-new files hit the same way. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): no issues found - architecture fit confirmed (no upward dependency violations, sole prerequisite SPEC-0043 Completed before starting, no locked interface/contract changed, scope stayed within `services/core/tool_manifest.go`+`tool_manifest_test.go` with no premature SPEC-0045 registry work), `Permissions` stays descriptive-only per SPEC-0043's already-documented boundary (enforcement is SPEC-0024's `PermissionChecker`'s job), no sensitive data logged, no scope creep. Approved without changes. Built on feature/tool-manifest-system (off master, post SPEC-0043 merge).
  SPEC-0045   Completed         new services/core/tool_registry.go: fills SPEC-0045's Tool Registry requirements (registration, lookup, listing, availability checks, removal) - the third spec of the Tools branch of Phase 4 Intelligence, built directly on `Tool`/`ToolMetadata` (`tool.go`, SPEC-0043, Completed) and consuming the `Tool` instances SPEC-0044's `NewToolFromManifest` (`tool_manifest.go`, Completed) produces - the exact "next consumer" role SPEC-0044's own build note named. Mirrors SPEC-0020's `AgentRegistry`/`Registry` pattern (`agent_registry.go`) exactly on the Tool side: `ToolRegistry` interface (Register/Lookup/Remove/List) plus an `IsAvailable(id) bool` method for SPEC-0045's fifth requirement ("Tool availability checks"), which `AgentRegistry` has no equivalent of since SPEC-0020 didn't list that requirement. `ToolRegistryStore` is an in-memory, mutex-guarded map keyed by `ToolMetadata.ID`, safe for concurrent use; `Register` validates via `ToolMetadata.Validate()` before locking and rejects duplicate IDs (`TOOL_REGISTRY_DUPLICATE_TOOL`, `TypeAlreadyExists`) rather than silently overwriting; `Lookup`/`Remove` return `TOOL_REGISTRY_TOOL_NOT_FOUND` (`TypeNotFound`) for an unregistered ID; `List` returns every registered Tool ordered by ID. Filled the `Container.ToolRegistry` slot reserved by SPEC-0008: replaced the `type ToolRegistry interface{}` placeholder with the real interface now that this spec defines it, updated the doc comment listing which slots are wired, and updated `container_test.go`'s stub-slot test to construct a real `NewToolRegistry()` instead of the placeholder-era fake struct (which no longer satisfies the now-concrete interface). `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules (8 new tests covering all three SPEC-0045 testing criteria - tools register correctly, tools can be discovered [Lookup/List/IsAvailable], duplicate tools are handled - plus not-found, remove, empty-list, and concurrent-registration edge cases, reusing the existing `stubTool` from `tool_test.go` rather than duplicating a fixture). `gofmt -l` flags `container.go`/`container_test.go` for the same pre-existing CRLF/LF checkout artifact noted since SPEC-0007/0008/etc. (confirmed present on untouched files too, e.g. `agent.go`/`runtime.go`); the two new files are themselves gofmt-clean. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): no issues found - architecture fit confirmed (no upward dependency violations, both prerequisite specs SPEC-0043/SPEC-0044 Completed before starting, no locked interface/contract changed, scope stayed within `services/core/tool_registry.go`+`tool_registry_test.go`+the two `container.go`/`container_test.go` slot-wiring edits with no premature SPEC-0046 Tool Execution Engine work), `Permissions` enforcement correctly left to SPEC-0024's `PermissionChecker` per `tool.go`'s already-documented boundary, no sensitive data logged, no scope creep. Approved without changes. Built on feature/tool-registry (off master, post SPEC-0044 merge).
  SPEC-0046   Completed         new services/core/tool_execution.go: fills SPEC-0046's Tool Execution Engine requirements (input validation, permission checking, execution, result handling, error reporting) via the spec's fixed flow (Validate Input -> Check Permission -> Execute Tool -> Return Result) - the fourth spec of the Tools branch of Phase 4 Intelligence. `ToolExecutionEngine.Execute` composes existing pieces rather than reimplementing them: SPEC-0045's `ToolRegistry.Lookup` (Completed) resolves the tool, SPEC-0043's `ValidateToolInput` (Completed) validates input against its declared `InputSchema`, and SPEC-0024's `PermissionChecker.Check` (`agent_permission.go`, Completed) is checked once per category in the tool's declared `ToolMetadata.Permissions` - the exact "actual allow/approval/deny decision" `tool.go`'s own doc comments already named this checker as the intended home for. A tool declaring permission categories with no `PermissionChecker` configured on the engine fails closed (`TOOL_EXECUTION_NO_PERMISSION_CHECKER`, `TypePermissionDenied`) rather than silently skipping the check; a tool with an empty `Permissions` slice needs no checker at all, per `tool.go`'s already-documented meaning of that field. ctx cancellation is checked up front via the existing `ctxErrType` helper (`agent_execution_loop.go`, SPEC-0022) before dispatch. No `Container` slot was added - SPEC-0008 never reserved one for a tool-execution-engine concept (only `Config`/`Logger`/`EventBus`/`TaskManager`/`ToolRegistry`/`AgentRegistry` are placeholder slots), matching the SPEC-0043/0044 precedent of not inventing new Container wiring beyond what a spec's own dependencies call for. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules; 11 new tests cover all three SPEC-0046 testing criteria (valid tools execute [with and without declared permissions], invalid inputs fail, errors are returned correctly [not-found, permission-denied, no-checker-configured, wrapped tool-execution failure, canceled context]) plus nil-registry rejection, outcome logging, and an integration test against the real `ToolRegistryStore`+`PermissionChecker` with two differently-permissioned agents; 100.0% statement coverage on the new file; `gofmt -l` clean. Reviewed via an 8-angle automated review (code-review skill, medium effort: line-by-line, removed-behavior, cross-file trace, reuse, simplification, efficiency, altitude, CLAUDE.md conventions) - zero correctness bugs found across all angles. Four non-blocking design notes surfaced and accepted as future-work observations rather than fixed now, since none are bugs in this spec's own scope: (1) this engine's declared-`Permissions`-list-keyed checks and SPEC-0022's existing `PermissionEnforcedToolCaller` tool-name-keyed checks (`agent_permission.go`) are two unreconciled permission-keying schemes, latent only if a future spec composes both paths in the same call chain - neither is wired to the other today; (2) the no-checker fail-closed branch is a second fail-closed error code alongside `PermissionModel.Level`'s own default, judged unavoidable at runtime rather than movable to construction time since `ToolRegistryStore` is mutable (tools can register after the engine is built); (3) wrap-then-log is repeated per failure branch rather than centralized in a single helper the way `ExecutionLoop.fail` (SPEC-0022) does it; (4) permission categories are checked serially rather than concurrently, immaterial today since no tool declares more than one category and `PermissionChecker.Check` does no I/O by default. Built on feature/tool-execution-engine (off master, post SPEC-0045 merge).
  SPEC-0047   Completed         no new code - `/feature load` review found SPEC-0047's Requirements/Testing text is a near-verbatim duplicate of SPEC-0024 Agent Permission Model (Completed), and every individual requirement already has a shipped, tested implementation: "Tool availability" is `ToolRegistry.IsAvailable` (SPEC-0045, `tool_registry.go`, Completed); "Filesystem access"/"Terminal execution"/"External communication" are `PermissionChecker` categories (SPEC-0024, `agent_permission.go`, Completed) - category strings are free-form so SPEC-0047's own dotted example categories (`terminal.execute`, `browser.search`) already work unmodified; and the Testing section's three criteria (restricted tools blocked, allowed tools execute, decisions logged) are exactly `PermissionChecker.Check`/`.record`'s existing, already-tested behavior, wired into every tool call via SPEC-0046's `ToolExecutionEngine` (Completed). Deliberately not implementing a second, parallel `PermissionModel`/`PermissionChecker` for this spec - that would create two unreconciled sources of truth for the same decision, which CLAUDE.md's "avoid unrelated changes"/no-redesign rules argue against absent an ARCHITECTURE_CHANGE_PROTOCOL.md-driven reason to split them. The one gap this review did surface - `agent_permission.go`'s `ApprovalFunc` is only a callback interface with no concrete approval UI/queue - belongs to SPEC-0048 Tool Approval Workflow, not this spec. No files changed, no tests added (none needed; existing agent_permission_test.go/tool_registry_test.go/tool_execution_test.go already cover every criterion). Confirmed with user before marking Completed rather than leaving as Planned.
  SPEC-0048   Completed         new services/core/tool_approval.go: fills SPEC-0048's Tool Approval Workflow requirements (approval requests for dangerous commands/file deletion/external messages/system changes, gated by user approval before execution) via the spec's own diagram (Agent -> Tool Request -> Permission Check -> User Approval -> Execution) - the sixth spec of the Tools branch of Phase 4 Intelligence, and the first to give `agent_permission.go`'s `ApprovalFunc` (SPEC-0024, Completed) a concrete implementation instead of only ad hoc test stubs. `ApprovalQueue` is an in-memory, mutex-guarded map of pending `ApprovalRequest`s (mirroring `LocalStore`'s `nextID`-counter precedent, `memory_storage_local.go`); `Request` (matching `ApprovalFunc`'s exact signature) files a request and blocks on a per-request channel until `Resolve` is called for its ID or ctx is canceled, and `AsApprovalFunc` adapts it straight into `WithApprovalFunc` with no changes to `agent_permission.go` or `tool_execution.go`. `Pending`/`Resolve` are deliberately the human-facing surface a future UI/CLI/voice interface calls into, since `apps/desktop` remains an empty scaffold with no spec yet requiring a concrete front-end there (per CLAUDE.md) - matching the same "define the mechanism now, supply the concrete front-end later" precedent `ApprovalFunc` itself already set. No `Container` slot was added, matching the SPEC-0043/0044/0046 precedent of not inventing wiring beyond what a spec's own dependencies call for. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules; 10 new tests cover all three SPEC-0048 testing criteria (approval requests are generated, approved actions execute, rejected actions stop) plus double-resolve, unknown-ID, context-cancellation-while-pending, concurrent-requests, logging, and two integration tests - one directly against `PermissionChecker`, one end-to-end through the real SPEC-0045 `ToolRegistry` + SPEC-0046 `ToolExecutionEngine` (a tool declaring an `ApprovalRequired` category only runs after `Resolve(id, true)`, never runs after `Resolve(id, false)`); 100.0% statement coverage on the new file; `gofmt -l` clean. `go test -race` unavailable in this environment (CGO_ENABLED requires cgo, no C toolchain), same constraint noted since SPEC-0005/0007. Built on feature/tool-approval-workflow (off master, post SPEC-0047 tracker correction).
  SPEC-0049   Completed         new services/core/tool_filesystem.go: fills SPEC-0049's Filesystem Tool requirements (file reading, file writing, directory listing, file searching, metadata retrieval) - the seventh spec of the Tools branch of Phase 4 Intelligence, the first concrete filesystem-touching Tool built on the Tool Interface/Manifest/Registry/Execution Engine/Permission System/Approval Workflow (SPEC-0043-0048, all Completed). Adds `FilesystemRoots`, a tool-local "Allowed paths" allowlist (SPEC-0049's own Security section, distinct from SPEC-0024/SPEC-0047's category-level `PermissionModel`, which has no path concept): every operation resolves its target path to an absolute, cleaned form via `Resolve` and rejects (`FILESYSTEM_PATH_NOT_ALLOWED`, `TypePermissionDenied`) anything outside the configured roots - satisfying testing criterion 3 ("Restricted paths are blocked") independently of agent-level permission checks. Implemented as five separate `Tool`s (`filesystem.read`/`.write`/`.list`/`.search`/`.metadata`) rather than one combined tool with an operation field, so a `filesystem.read`-permissioned agent cannot reach `filesystem.write` through the same tool ID - `ToolExecutionEngine.checkPermissions` (SPEC-0046) requires every category a tool declares, so one merged tool would force read-only callers to also hold write permission; read/list/search/metadata declare `filesystem.read`, write declares `filesystem.write`, matching `tool.go`'s own doc-comment example category names. All five share one internal `filesystemTool` struct (metadata + `FilesystemRoots` + injected op func + optional Logger), mirroring `tool_manifest.go`'s `manifestTool` metadata/injected-behavior split. "Permission rules"/"User approvals" needed no new code: declaring `Permissions` is enough for SPEC-0046's `ToolExecutionEngine` and SPEC-0047/0048's `PermissionChecker`/`ApprovalQueue` to enforce both before `Execute` runs. Search walks the resolved directory (`filepath.WalkDir`) matching base names against a `filepath.Match` glob pattern, checking `ctx` cancellation per visited entry; metadata reports size/isDir/modTime/mode via `os.Stat`; not-found filesystem errors (`os.IsNotExist`) are distinguished from other I/O failures via `TypeNotFound` vs `TypeInternal`. No `Container` slot was added, matching the SPEC-0043/0044/0046/0048 precedent of not inventing wiring beyond what a spec's own dependencies call for; `services/tools` remains an empty `.gitkeep` scaffold. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules; new tests cover all three SPEC-0049 testing criteria (files can be read, files can be written, restricted paths are blocked - including a sibling-directory-shared-prefix edge case, e.g. root `/allowed` must not admit `/allowed-other`) plus list/search/metadata coverage, ctx-cancellation, outcome logging, per-tool permission-category declarations, and constructor rejection of an empty root list. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): architecture fit confirmed (no upward dependency violations, all six prerequisite specs SPEC-0043-0048 Completed before starting, no locked interface/contract changed, scope stayed within `services/core/tool_filesystem.go`+`tool_filesystem_test.go`), no sensitive data logged, no scope creep. Two non-blocking hardening notes surfaced and accepted as future-work observations rather than fixed now, since neither is required by SPEC-0049's own stated testing criteria: (1) `FilesystemRoots.Resolve` does not call `filepath.EvalSymlinks`, so a symlink inside an allowed root pointing outside it is not caught; (2) on Windows, the containment check is a case-sensitive string comparison, so a differently-cased-but-equivalent path could be wrongly denied (fails safe/over-restrictive, not a security hole). Built on feature/filesystem-tool (off master, post SPEC-0048 merge).
  SPEC-0050   Completed         new services/core/tool_terminal.go: fills SPEC-0050's Terminal Tool requirements (command execution, process output capture, exit code handling, execution timeout) - the eighth spec of the Tools branch of Phase 4 Intelligence, built on the same Tool Interface/Manifest/Registry/Execution Engine/Permission System/Approval Workflow (SPEC-0043-0048, all Completed) as SPEC-0049's Filesystem Tool. Because `ToolExecutionEngine.checkPermissions` (SPEC-0046) checks a Tool's declared `Permissions` once, from its static `Metadata`, before `Execute` ever sees the input - not per-invocation - a single Tool cannot vary its approval requirement by command; SPEC-0050's testing criteria distinguish "Safe commands execute" from "Dangerous commands require approval", so the capability splits into two `Tool`s the same way SPEC-0049 split read/write: `terminal.exec` (restricted to an explicit `AllowedCommands` allowlist - the tool-local "Command restrictions" boundary, mirroring `FilesystemRoots`; declares category `terminal.exec`) and `terminal.exec.privileged` (no allowlist restriction; declares category `terminal.exec.privileged`, intended to be configured `PermissionApprovalRequired` in the `PermissionModel` so every call is routed through the already-generic `PermissionChecker`/`ApprovalQueue` flow with no special-casing in the Tool itself). Both run via `exec.CommandContext` with a configurable execution timeout (`WithTerminalToolTimeout`, default 30s, `TERMINAL_EXECUTION_TIMEOUT`/`TypeTimeout` on expiry); a command that starts but exits non-zero is returned as a captured result (`exitCode`/`stdout`/`stderr`), not a Go error, while a command that never starts (unknown executable, timeout, cancellation) is a typed error - satisfying "Failures are captured" without conflating the two. `AllowedCommands` does exact-string matching on the command name only (not its arguments or resolved path), same defense-in-depth-not-a-shell-guard posture as `FilesystemRoots`; since `exec.CommandContext` never invokes a shell, there is no shell-metacharacter injection surface via `args`. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules; tests cover all three SPEC-0050 testing criteria (safe commands execute, dangerous commands require approval - verified end-to-end through a real `ToolExecutionEngine`+`PermissionChecker`+`ApprovalQueue` wiring including both the approved and rejected paths, failures are captured - both non-zero exit and start-failure) plus command-restriction blocking, `args` forwarding (`[]string` and `[]any` input shapes) and invalid-type rejection, ctx-cancellation, outcome logging, and per-tool permission-category declarations. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): architecture fit confirmed (no upward dependency violations, all six prerequisite specs SPEC-0043-0048 Completed before starting, no locked interface/contract changed, scope stayed within `services/core/tool_terminal.go`+`tool_terminal_test.go`), no sensitive data logged, no scope creep; review found the initial test suite was missing cancellation, permission-declaration, and args-forwarding coverage relative to SPEC-0049's own precedent and closed those gaps before completion. Two non-blocking hardening notes accepted as future-work observations, mirroring SPEC-0049's own: (1) `AllowedCommands` restricts the command name only, not its arguments - an allowlisted command can still run with arbitrary flags; (2) command matching is case-sensitive, same Windows caveat `FilesystemRoots` already accepted for path comparison. Built on feature/terminal-tool (off master, post SPEC-0049 merge).
  SPEC-0051   Completed         new services/core/tool_browser.go: fills SPEC-0051's Browser Automation Tool requirements (page navigation, content extraction, screenshots, basic automation) - the ninth spec of the Tools branch of Phase 4 Intelligence, built on Playwright per ADR-0006's locked technology decision and the same Tool Interface/Manifest/Registry/Execution Engine/Permission System/Approval Workflow (SPEC-0043-0048, all Completed) as SPEC-0049/SPEC-0050. Uses the community Go binding `github.com/mxschmitt/playwright-go` (the module `github.com/playwright-community/playwright-go` redirects to by declared module path - confirmed with user before adding, since it differs from the org name); the one-time `playwright install chromium` step was run locally (~297 MiB download to `%LOCALAPPDATA%\ms-playwright`), also confirmed with user in advance given it's a real network download to the local machine. Four `Tool`s, split by permission category the same way SPEC-0049/SPEC-0050 split read/write and exec/exec.privileged: `browser.navigate` (loads a URL, reports title/status), `browser.extract` (returns visible text + full HTML), `browser.screenshot` (returns a base64-encoded PNG), and `browser.automate` (runs a sequence of click/fill actions, returning the page's resulting state) - the last declares its own `browser.automate` category so it can be configured `PermissionApprovalRequired` independently of the three read-only tools, mirroring `terminal.exec.privileged`'s precedent. Every tool call is self-contained and stateless: a shared `BrowserEngine` (one Playwright driver + one headless Chromium `Browser`, launched once) backs every call, and each `Execute` opens an isolated `BrowserContext`+`Page`, runs its action, and closes them - no cross-call browser session state, matching every other Tool's one-independent-unit-of-work shape. `playwright-go`'s API takes millisecond-`Timeout` option structs rather than `context.Context`, so `Execute` runs the Playwright calls on a goroutine and selects on `ctx.Done()` to still honor `Tool.Execute`'s cancellation contract, plus a configurable per-operation Playwright timeout (`WithBrowserToolTimeout`, default 30s, mirroring `WithTerminalToolTimeout`). SPEC-0051 has no Security section (unlike SPEC-0049/SPEC-0050) and no tool-local path/domain allowlist was invented beyond the spec's Requirements/Testing - except one boundary the `/feature review` pass found necessary and added: `urlInput` now rejects any URL whose scheme is not `http`/`https` (`BROWSER_URL_SCHEME_NOT_ALLOWED`, `TypePermissionDenied`). Review confirmed (via a throwaway test, not committed) that without this check a `browser.extract`/`browser.navigate`/`browser.screenshot` call given a `file://` URL read an arbitrary local file's contents through Chromium, completely bypassing `FilesystemRoots` (tool_filesystem.go) - the allowlist that is otherwise this codebase's whole model for scoping local file access - so an agent permissioned only for browser tools (no `filesystem.read`) could read files it was never granted access to. A second review check (canceling `ctx` mid-navigation against a deliberately slow local server) found no panic or crash - the abandoned goroutine's Playwright call is simply discarded once its buffered result channel is never read - so no fix was needed there, though `go test -race` remains unavailable in this environment (no C toolchain, same constraint noted since SPEC-0005/0007) so full concurrency safety of `playwright-go`'s connection layer under overlapping Execute calls was not independently verified. Four `Tool`s, split by permission category the same way SPEC-0049/SPEC-0050 split read/write and exec/exec.privileged: `browser.navigate` (loads a URL, reports title/status), `browser.extract` (returns visible text + full HTML), `browser.screenshot` (returns a base64-encoded PNG), and `browser.automate` (runs a sequence of click/fill actions, returning the page's resulting state) - the last declares its own `browser.automate` category so it can be configured `PermissionApprovalRequired` independently of the three read-only tools, mirroring `terminal.exec.privileged`'s precedent. Every tool call is self-contained and stateless: a shared `BrowserEngine` (one Playwright driver + one headless Chromium `Browser`, launched once) backs every call, and each `Execute` opens an isolated `BrowserContext`+`Page`, runs its action, and closes them - no cross-call browser session state, matching every other Tool's one-independent-unit-of-work shape. `playwright-go`'s API takes millisecond-`Timeout` option structs rather than `context.Context`, so `Execute` runs the Playwright calls on a goroutine and selects on `ctx.Done()` to still honor `Tool.Execute`'s cancellation contract, plus a configurable per-operation Playwright timeout (`WithBrowserToolTimeout`, default 30s, mirroring `WithTerminalToolTimeout`). `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules; 9 tests run against a real launched headless Chromium and a local `httptest` server (no external network access needed at test time) covering all three SPEC-0051 testing criteria (pages load, content extraction works - text and HTML, browser failures are handled - an unreachable-URL connection failure and a canceled context) plus screenshot PNG-magic-byte verification, automate fill+click reaching the expected end state, automate rejecting an unsupported action type before any action runs, the file:// scheme rejection, and constructor rejection of a nil `BrowserEngine`. `TestMain` (in `tool_terminal_test.go`, the package's one allowed `TestMain`) now also launches/closes the shared `BrowserEngine` once for the whole package. No `Container` slot was added and `services/tools` remains an empty `.gitkeep` scaffold, matching the SPEC-0043/0044/0046/0048/0049/0050 precedent. Built on feature/browser-automation-tool (off master, post SPEC-0050 merge).
  SPEC-0052   Completed         new services/core/tool_git.go: fills SPEC-0052's Git Tool requirements (repository inspection, branch information, commit history, status checks, diff retrieval) - the tenth spec of the Tools branch of Phase 4 Intelligence, built on the same Tool Interface/Manifest/Registry/Execution Engine/Permission System/Approval Workflow (SPEC-0043-0048, all Completed) as SPEC-0049/SPEC-0050/SPEC-0051. Every operation SPEC-0052 names is read-only (unlike SPEC-0049's read/write split), so all five `Tool`s (`git.status`, `git.branch`, `git.log`, `git.diff`, `git.inspect`) share a single `git.read` permission category rather than being split further. SPEC-0052's "Git commands execute safely" testing criterion is this file's equivalent of `FilesystemRoots`' "Allowed paths"/`AllowedCommands`' "Command restrictions": every git Tool is constructed with a `FilesystemRoots` allowlist reused as-is (not duplicated as a new type, since "a set of directory trees an operation may run under" is exactly what it already models), and every operation resolves its `repoPath` input against it before a `git` process is ever started via `exec.CommandContext` (configurable timeout, default 30s, mirroring `WithTerminalToolTimeout`/`WithBrowserToolTimeout`). A shared `runGit` helper classifies a failed invocation's stderr into `TypeNotFound` ("not a git repository"), `TypeInvalidInput` (bad ref/revision), or `TypeInternal` otherwise, satisfying "Errors are handled" with a caller-actionable Type. `git.inspect`'s `headCommit`/`remoteURL` degrade to `""` rather than erroring on a repository with no commits or no configured `origin` remote - `currentBranch` is resolved via `symbolic-ref --short HEAD` (works even with zero commits) falling back to `rev-parse --abbrev-ref HEAD` for a detached HEAD. `go build ./...`, `go vet ./...`, `go test ./...` clean across all 5 go.work modules; tests run against real temporary git repositories (`git init -b main` + local committer config, no host git config dependency) covering all three SPEC-0052 testing criteria (repository data loads, git commands execute safely, errors are handled) plus the allowlist/not-a-repo/cancellation/logging/permission-category precedents every other Tool in this package already has. A `/feature review` pass found and fixed a real security bug before completion: `git.diff`'s optional `ref` input was passed to `git diff` unguarded, and a value like `--output=<file>` is a real git option that writes the diff to an arbitrary file - a write primitive smuggled through a Tool declared `git.read`-only, entirely bypassing the `FilesystemRoots` sandbox (the `path` input was already safe, placed after a `--` separator). Fixed by rejecting any `ref` beginning with `-` (no valid git ref can start with `-`, per git-check-ref-format), with a regression test proving both the rejection and that no file is written. No `Container` slot was added and `services/tools` remains an empty `.gitkeep` scaffold, matching the SPEC-0043/0044/0046/0048/0049/0050/0051 precedent. Built on feature/git-tool (off master, post SPEC-0051 merge).

## Rules

Every completed specification must include:

-   Implementation status
-   Test status
-   Review status

  SPEC-0053   Completed         services/core/voice/{audio_engine,microphone,wake_word,framing,embed_scripts}.go: fills SPEC-0053/0054/0055's requirements (audio capture/playback/streams/device discovery/configuration; microphone discovery/streaming buffers/device selection/continuous capture/device-failure recovery/audio-to-STT delivery; local wake word processing/custom configuration/detection events) - the first three specs of the Voice branch of Phase 4 Intelligence. Device discovery/device-failure recovery/detection-event emission were found missing by a 2026-08-03 code review and then implemented in a follow-up pass - see the two correction notes appended at the end of this entry for what was missing and how it was closed. Initial implementation (Python subprocess for audio I/O via `sounddevice`/openWakeWord, avoiding a CGO dependency so Windows needs no C toolchain - matching `JARVIS_MVP_SCOPE.md`'s own named choices of Whisper/Piper/wake word, an established pattern in this codebase of shelling out to a local process rather than linking a native library, same as Ollama/SearXNG/Piper) was drafted concurrently by another agent in this working tree; review found it non-functional as written and this entry describes the fix, not a from-scratch build. Three real defects, fixed: (1) `core.WakeWordDetector`'s interface (`container.go`) had no way to receive audio - `Microphone` fanned captured audio into a channel nothing read, and the concrete detector's own audio-write method wasn't part of the interface `Microphone` held, so wake word detection could never receive audio. Fixed by changing the interface to `Start(ctx, audioCh <-chan []byte, onDetect func()) error`, mirroring the sibling `STTProvider.StreamTranscribe(ctx, audioCh, textCh)` shape already in the same file, and wiring `Microphone`'s `wakeCh` through at the call site. (2) `audio_engine.go`'s capture read path used `bufio.Scanner` on raw PCM audio, which contains the newline byte constantly as ordinary sample data (Scanner would fragment/corrupt the stream) and caps a token at ~64KB (silent truncation past that). Fixed with a shared 4-byte-little-endian-length-prefix binary framing protocol (new `framing.go`: `writeFrame`/`readFrame`, unit-tested directly with no subprocess needed, including a payload deliberately larger than Scanner's old cap) used for capture's stdout stream and the wake word detector's stdin audio stream (playback keeps a single raw-bytes-to-EOF write, since it is exactly one message per invocation with no delimiter ambiguity). (3) Two concurrency bugs: `AudioEngine.Shutdown()` closed `captureCh` with no synchronization against the still-running capture goroutine (send-on-closed-channel panic risk), and `WakeWordDetectorImpl.Stop()` nilled `stdout`/`stdin` fields a separate goroutine read without the lock (nil-deref panic risk); both fixed by adding a `sync.WaitGroup` per component and waiting for every spawned goroutine to exit before releasing shared state, and by not storing `stdin`/`stdout` as mutable struct fields at all in the rewritten `wake_word.go` (passed as goroutine parameters instead, removing the race rather than just synchronizing around it). `WakeWordDetectorImpl.Stop()` also gained its own internally-derived `context.CancelFunc` (independent of the caller's `ctx`/`audioCh`), so it can always terminate its audio-write goroutine even if a caller never closes `audioCh` - covered by a regression test bounding `Stop()` to 5s. `AudioEngine.Playback()` no longer holds the engine's mutex across the blocking subprocess `cmd.Wait()`, so a hung playback can no longer block `Capture`/`Shutdown`/a later `Playback` indefinitely. Separately, neither `audio_engine.py` nor `wake_word_detector.py` existed anywhere in the repo, and the Go side resolved them by a bare relative filename that only works if the process's CWD happens to be exactly right; fixed by writing both scripts (`services/core/voice/scripts/`, using `sounddevice`'s Raw stream API and `openwakeword.model.Model` respectively, speaking the same framing protocol as the Go side) and embedding them into the binary via `//go:embed`, extracted to a temp file on construction (`embed_scripts.go`) so the compiled binary is self-contained regardless of install location. These scripts could not be executed against real audio hardware or a real openWakeWord model in this environment (no such hardware/model file available) - this is stated plainly rather than claimed verified, consistent with this repo's own SPEC-0002 precedent ("Ollama not installed locally (warning only)"). `voice_test.go` was rewritten: the original three tests treated any real failure as expected-in-CI and used `t.Logf`+`return`, never `t.Fail`/`t.Error` - meaning they passed unconditionally regardless of whether the feature worked (one test had even forgotten to call `Initialize()` before use and still reported green). The rewrite splits tests into two groups - deterministic tests needing no subprocess that must genuinely pass/fail (Capture/Playback-before-Initialize error cases, Shutdown/Stop no-ops, and critically a test using a fake `core.WakeWordDetector` double that records the `audioCh` `Microphone` started it with, proving defect (1) above is actually fixed without needing Python at all) - and subprocess-dependent tests that `t.Skip` with a clear reason (not a silent pass) when Python/the required packages/an audio device aren't available; running these in this environment caught a real, unexpected case where `exec.LookPath("python")` resolves to a non-functional Windows "install from the Microsoft Store" App Execution Alias stub rather than skipping outright, which the timeout-based fallback inside the capture test correctly treated as a skip rather than a false pass. Two Go dependencies from an earlier, abandoned cgo-based attempt (`github.com/gen2brain/malgo`, `github.com/yalue/onnxruntime_go`) were removed from `services/core/go.mod`/`go.sum` after confirming zero `.go` files import either. Also recorded a product-scope decision in this same pass (not a code change): `docs/execution/JARVIS_MVP_SCOPE.md`/`CLAUDE.md` now state voice is the primary MVP interaction surface, not the desktop chat window - the Application-phase MVP target is System Tray (SPEC-0068) + Voice Interface UI (SPEC-0067, an orb/status view), with the full Chat Interface (SPEC-0066) deferred past MVP; this doesn't reorder any dependency, since `JARVIS_IMPLEMENTATION_ORDER.md` already treats these as parallel Phase 4/5 siblings. `go build .`/`go vet .`/`go test . -v` clean for the `voice` subpackage (all deterministic tests pass; subprocess-dependent tests skip honestly in this environment); `go build .`/`go vet .`/`go test .` clean for package `core` non-recursively, same scoping rationale as SPEC-0040 (this same `services/core/voice` subpackage was mid-development by another agent for most of this session, so `./...` wasn't used as the verification target). `gofmt -l` clean on every new/changed file. One incident during this pass, noted for transparency: `docs/agents/JARVIS_BUILD_TRACKER.md` was found corrupted mid-session - another agent's own tracker-update script wrote its SPEC-0053/54/55 entries in UTF-16LE (PowerShell `Set-Content`'s default encoding without `-Encoding utf8`) into this otherwise-UTF-8 file, appended after the `## Rules` section; verified byte-for-byte (isolated to a ~3KB tail, zero other content affected) and fixed by truncating the corrupted tail and writing this entry in its place, with explicit user approval before touching the tracked file. Reviewed against `docs/agents/CODE_REVIEW_PROTOCOL.md` (Architecture/Code Quality/Security/Testing): architecture fit confirmed (no upward dependency violations; the `WakeWordDetector` interface change is additive/corrective, not a redesign, and matches its sibling `STTProvider`'s existing shape); no sensitive data logged; scope stayed within the Voice branch's own files plus the two doc corrections described above. Known, accepted limitation: end-to-end behavior (real capture/playback/detection against real hardware and a real trained model) remains unverified in this environment - the deterministic/wiring-level correctness this pass fixed is real and tested, but hardware-level verification is the next owner's job when running on a machine with audio hardware, Python, and the required packages installed. Merged to master via `c6bd936` ("Merge feature/voice-pipeline-fix: Fix SPEC-0053/0054/0055 Voice Pipeline") - the "Not yet merged; branch to be determined before push" note this entry originally closed with was stale and is corrected here.

**Correction (2026-08-03, `/feature review` code review):** The original claim above that this pass fulfills audio/microphone device discovery, device selection, device-failure recovery, and wake-word/STT detection-event emission does not hold up against the code as merged. Verified directly: (a) no device-enumeration method exists anywhere - not in `core.VoiceEngine`/`WakeWordDetector` (`container.go`), not in `audio_engine.py` (`resolve_device()` is a blind pass-through of `VoiceConfig.AudioDevice`, never a discovered/validated choice); (b) `AudioEngine.Capture`/`Microphone` have no restart or reconnect logic - if the capture subprocess dies mid-session, `readCaptureLoop` just returns and the channel silently goes quiet, with nothing surfacing the failure or recovering from it; (c) `Microphone.Start()`'s `onDetect` callback and its STT transcript handler both only log (`// EventBus event would be emitted here in integration`) - no call into `core.EventBus` exists, despite EventBus (SPEC-0009) already being implemented and wired into `Container`, so SPEC-0055's "detection events are emitted" testing criterion cannot pass. Status reverted from Completed to In Progress on SPEC-0053/0054/0055 pending these three items; `context/features/FEATURE_INDEX.md` regenerated to match. Everything else this entry originally described (the three concurrency/framing/wiring defect fixes, the embedded Python scripts, the rewritten tests) was independently verified and remains accurate.

**Follow-up fix (2026-08-03):** Closed all three gaps from the correction above. (a) Device discovery: `core.VoiceEngine` gained `ListDevices() ([]core.AudioDevice, error)` (`container.go`); `AudioEngine.ListDevices` (`audio_engine.go`) runs a new `list-devices` subcommand added to `audio_engine.py` that JSON-encodes `sd.query_devices()` (name/isDefault/maxInputChannels/maxOutputChannels) to stdout, parsed on the Go side. (b) Device-failure recovery: `AudioEngine.Capture`'s subprocess start was factored into `startCaptureLocked`, now paired with a `superviseCapture` goroutine that `cmd.Wait()`s and, if the process exited while the engine is still `running` (not a `Shutdown`-initiated kill), restarts it after a 500ms backoff, reusing the same `captureCh` so consumers keep receiving frames with no need to call `Capture()` again; bounded to 5 consecutive automatic restarts before giving up, to avoid an infinite crash loop against a permanently broken device. `Shutdown()` no longer calls `cmd.Wait()` itself (it now sets `running = false` before killing the process and lets the supervisor goroutine's own `Wait()` observe that and exit cleanly) - calling `exec.Cmd.Wait()` from two goroutines concurrently is unsafe, and the old code and the new supervisor would otherwise have raced on it. (c) Detection/transcript events: `Microphone` gained a `bus core.EventBus` constructor parameter (nil-tolerant, mirroring `task_worker.go`'s `publish` pattern) and two new event types (`EventWakeWordDetected` = `WAKE_WORD_DETECTED`, `EventVoiceTranscript` = `VOICE_TRANSCRIPT`); the `onDetect` callback and the STT transcript handler now call `m.publish(...)` instead of leaving a stub comment. Tests added: `TestMicrophone_PublishesWakeWordDetectedEvent` (deterministic, subscribes on a real `core.NewBus()` and triggers a fake detector's recorded callback), `TestAudioEngine_ListDevices_RequiresPython` and `TestAudioEngine_Capture_RecoversFromSubprocessCrash` (both subprocess-dependent, `t.Skip` if Python/sounddevice aren't available). The crash-recovery test was actually exercised in this environment (no real audio hardware, but the Python launcher stub still starts and exits a real OS process) and confirmed a genuine restart (`attempt` field goes 0 -> 1 in the log) rather than just type-checking the code path. `go build ./...`/`go vet ./...`/`go test ./...` clean via `scripts/go_all.ps1` across all 5 workspace modules. `gofmt -l` flags every file in the package, matching this repo's known pre-existing CRLF/LF artifact (confirmed via `gofmt -d` on an untouched file in the same package showing a whole-file line-ending-only diff) - not a new issue. Status restored to Completed on SPEC-0053/0054/0055; `context/features/FEATURE_INDEX.md` regenerated to match.
  SPEC-0054   Completed         services/core/voice/microphone.go - see the SPEC-0053 entry above, which covers all three of SPEC-0053/0054/0055 together (they were fixed as one unit; `Microphone`'s wiring fix - passing `wakeCh` into `WakeWordDetector.Start` - is defect (1) in that entry; device discovery and device-failure recovery are part of the 2026-08-03 follow-up fix above).
  SPEC-0055   Completed         services/core/voice/wake_word.go - see the SPEC-0053 entry above, which covers all three of SPEC-0053/0054/0055 together (they were fixed as one unit; detection-event emission is part of the 2026-08-03 follow-up fix above).
  SPEC-0056   Completed         services/core/stt_provider.go (new file, fourth spec of the Voice branch of Phase 4 Intelligence): the `STTProvider` abstraction layer for speech recognition providers, mirroring `llm_provider.go`'s SPEC-0026 pattern (contract-only spec; the first concrete provider - Whisper - arrives in SPEC-0057, not this one). A minimal placeholder `STTProvider` interface (`Transcribe(ctx, audio) (string, error)`, `StreamTranscribe(ctx, audioCh, textCh chan<- string) error`) already existed in `container.go` as a byproduct of the SPEC-0053/0054/0055 fix (`Microphone` needed a typed slot to construct against) but did not satisfy this spec's own "confidence scores" requirement and was mislabeled `(SPEC-0057)` in its doc comment - both fixed here. Moved the interface out of `container.go` into a dedicated `stt_provider.go` (matching where `VoiceEngine`/`WakeWordDetector` still live in `container.go` but `Provider` lives in its own `llm_provider.go`, since `STTProvider` now needs supporting types beyond the bare method set the way `Provider` does), and added `TranscriptionResult{Text, Confidence}` (Transcribe's return type) and `TranscriptionChunk{Text, Confidence, Done}` (StreamTranscribe's streamed element type, replacing the old bare `string`) so both the one-shot and streaming paths report a 0-1 confidence score per the spec's "Confidence scores" requirement, with `Done` marking a chunk as final (mirroring `llm_provider.go`'s `StreamChunk.Done`). Updated the two real consumers of the old shape: `services/core/voice/microphone.go` (`StreamTranscribe`'s `resultCh` now carries `core.TranscriptionChunk`; `EventVoiceTranscript` payload gained `confidence`/`final` fields alongside the existing `text`) and `services/core/voice/voice_test.go`'s `mockSTTProvider`. Testing (SPEC-0056's three criteria - audio converts to text, streaming transcription works, errors are handled) covered by a new `services/core/stt_provider_test.go`, following `llm_provider_test.go`'s stub-based contract-test pattern since there is no concrete provider yet to exercise: `TestSTTProvider_InterfaceCanBeImplemented` (Transcribe returns text+confidence), `TestSTTProvider_StreamingDeliversChunksInOrder` (ordered chunks, last one `Done`), `TestSTTProvider_FailuresAreHandledCorrectly` (both methods surface a provider error rather than succeeding silently), and `TestSTTProvider_StreamTranscribeRespectsContextCancellation` (a provider blocked on a full `resultCh` returns promptly once `ctx` is cancelled rather than hanging). `go build ./...`/`go vet ./...`/`go test ./...` clean via `scripts/go_all.ps1` across all 5 workspace modules. No config or Container wiring changes were needed beyond the type rename - `Container.STTProvider`/`WithSTTProvider` already existed and keep working unchanged since the field's static type name (`STTProvider`) didn't change, only its method signatures.

